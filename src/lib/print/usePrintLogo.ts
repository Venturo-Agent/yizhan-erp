/**
 * usePrintLogo - Hook for loading company logo
 * 目前回空字串、列印不顯示 logo（未啟用 workspace logo 功能）
 */

/* eslint-disable @typescript-eslint/no-unused-vars */
export const usePrintLogo = (_isOpen: boolean): string => {
  return ''
}
