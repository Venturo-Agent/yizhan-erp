# UI 盤點：`/shared-data/attractions`

> 掃描日期：2026-05-26 ｜ 來源檔：`src/app/(main)/shared-data/attractions/page.tsx`（無 _components）
> 頁面類型：`占位 / WIP（尚未開發的功能說明頁）`

## 一句話用途
景點圈管理的「第二刀待做」占位頁。目前只是一張說明卡、列出未來要做的品管 UI（待審/已入池清單、完整度分數、批次蓋章），尚無任何實際功能。

## Layout 骨架
- **頁面框架**：`ContentPageLayout`（`title="景點圈管理"` 寫死、無 breadcrumb）
- **頁首**：標題硬編碼字串「景點圈管理」🔴（未走 i18n、與其他頁用 `t()` 不一致）；無麵包屑；無動作按鈕
- **分頁**：無

## UI 元素清單

### 🔘 按鈕 Buttons
| 位置/用途 | 用什麼組件 | variant | 尺寸 | 圖示 | 顏色 token | 對齊標準? |
|---|---|---|---|---|---|---|
| 無 | — | — | — | — | — | — |

### ⌨️ 輸入框 Input / Textarea
| 位置/用途 | 用什麼組件 | 類型 | 顏色/邊框 token | 對齊標準? |
|---|---|---|---|---|
| 無 | — | — | — | — |

### 🔽 下拉 / 選擇 Select / ComboBox / Dropdown
| 位置/用途 | 用什麼組件 | 樣式 | 對齊標準? |
|---|---|---|---|
| 無 | — | — | — |

### ☑️ 勾選 / 開關 Checkbox / Radio / Switch / Toggle
| 位置/用途 | 用什麼組件 | 對齊標準? |
|---|---|---|
| 無 | — | — |

### 📋 表格 / 列表 Table / List
| 用什麼組件 | 行高/密度 | 表頭樣式 | 斑馬紋 | 空狀態 | 對齊標準? |
|---|---|---|---|---|---|
| 無（只有一個 `<ul list-disc>` 條列未來計畫） | — | — | — | — | — |

### 🪟 對話框 Dialog / Drawer / Popover
| 位置/用途 | 用什麼組件 | level | footer 按鈕樣式 | 對齊標準? |
|---|---|---|---|---|
| 無 | — | — | — | — |

### 🏷️ 狀態標籤 Badge / Status / Tag / Chip
| 位置/用途 | 用什麼組件 | 形狀 | 顏色 token | 對齊標準? |
|---|---|---|---|---|
| 無 | — | — | — | — |

### 🎨 圖示 Icons
| 動作/用途 | icon 名 | 尺寸寫法 | 顏色 token |
|---|---|---|---|
| 無（不 import lucide） | — | — | — |

### 🃏 卡片 / 容器 Card / Panel / Tabs / Accordion
| 位置/用途 | 用什麼組件 | 圓角 token | 陰影 token | 對齊標準? |
|---|---|---|---|---|
| 說明卡 | 手刻 `<div rounded-lg border bg-card p-6>` | `rounded-lg` | 無 | 🟡 手刻、非 `<Card>` 組件 |

### 🔔 回饋 Toast / 確認框 / 空狀態 / 載入 Loading / Skeleton
| 位置/用途 | 用什麼組件 | 對齊標準? |
|---|---|---|
| 無 | — | — |

## 🔴 不統一 / 異常標記（重點、給最後彙整用）
- 🔴 **占位頁（WIP）**：標題明寫「WIP — 第二刀做品管 UI」、無實際功能。對外導覽（shared-data 入口）已放此卡、但點進來是空殼。
- 🔴 標題「景點圈管理」硬編碼中文、未走 `t()`（其他 shared-data 頁全走 i18n）。
- 🟡 說明卡用手刻 `<div bg-card>`、非 `<Card>` 組件。

## 備註
- 全頁無互動元素、無資料讀取、無權限判斷。
- 註解說明此頁未來要做：待審清單 / 已入池清單 / 完整度分數 / 批次蓋章退章。
- 健檢層面建議標記為「半成品 module」、避免被當 active 頁評分。
