# UI 統一執行報告 — 2026-05-26

> William 拍板統一決策後第一批執行。驗證：`npm run type-check` 0 error、`npm run lint` 0 error。

---

## ✅ 今晚完成（已驗證、type-check + lint 雙綠）

### 1. 共用組件（改一處、全站見效）
| 項目 | 改了什麼 | 檔案 |
|---|---|---|
| **Checkbox 勾變大** | 框 `h-4→h-5`、勾選符號放大到 18px + 加粗 `strokeWidth 3.5`（配色本就是 morandi 金） | `src/components/ui/checkbox.tsx` |
| **分頁選中頁金漸層** | 選中頁 `soft-gold` 實色 → `default`（`--btn-primary-bg` 金漸層）、不再純色 | `src/components/ui/enhanced-table/TablePagination.tsx` |

### 2. 圖示收斂（跨檔替換、import + JSX 全改）
| 用途 | 統一成 | 取代掉 | 動了幾檔 |
|---|---|---|---|
| 編輯 | `Edit` | Edit2 / Pencil / SquarePen / PenLine / FileEdit | 26 檔 |
| 設定 | `Settings` | Settings2 / Sliders / Wrench | 4 檔 |
| 錯誤/警告 | `AlertTriangle` | AlertCircle | 24 檔 |
| 圈勾確認 | `CheckSquare` | CheckCircle / CheckCircle2 | 多檔 |

---

### 3. 顏色統一（第二批、type-check 綠）
| 項目 | 改了什麼 | 範圍 |
|---|---|---|
| **StatusBadge 病根** | 組件內部 success/danger 由美術色 `morandi-green/red` → 語意色 `status-success/danger`（預設主題視覺不變、換主題自動對齊） | `status-badge.tsx`（改 2 行、全站 badge 受益） |
| **Tailwind 預設色硬傷** | 22 檔逐處判斷語意換 token（成功→success / 危險→danger / 警告→warning / 優先級高中低→danger/warning/neutral / 訂單狀態→對應 status） | orders、todos、hr×4、workspaces/[id]、finance reports、ai、shared-data、p/tour、not-found、global-error、bank-combobox 等 22 檔 |

清理後殘留（**都不該清**）：channel 品牌色（LINE/FB/IG、合法例外）、global-error 深色 scaffold（待定）。

---

## ⏳ 待續（明天 / 後續批次）

### 等你拍板
- **按鈕五大變體**：你說「先記錄、暫不動」→ 待拍板保留哪幾個 variant
- **純勾 `Check`（47 處）**：很多是「已完成/成功」行內標記、非確認按鈕。先保留、待你看 CheckSquare 效果後決定要不要連純勾也換
- 圖示未點到的三組（刪除/關閉、載入、展開）：先沿用主流 Trash2+X / Loader2 / Chevron、待你確認

### 大工程（逐頁、需分批）
- **原生控件換共用組件**：16 個原生 `<select>` → `<Select>`、24 個原生 checkbox → `<Checkbox>`、**新建 RadioGroup 組件**收 4 處原生 radio（重災區：orders 全家、finance/settings、hr 員工表單、visas、pay 公開頁）
- **StatusBadge 病根**：組件內部 success/danger 用美術色 `morandi-green/red` → 改 `status-success/danger`（**改 2 行、全站 badge 一次對齊**、顏色統一核心）
- **Tailwind 預設色硬傷**：orders OrderStatusBadge、todos、workspaces/[id] 等 9 檔
- **硬編卡片**：workspaces/[id] 整頁 `rounded-[24px]` 漸層卡（eslint-disable 壓）
- **手刻按鈕**：認證頁（login/change/reset）三套 inline、行動版 /app 整套
- **效能 bug**：shared-data/airports 6000+ 筆無分頁
- **失效 class**：`bg-status-danger-bg0`（ai 頁 3 處）、visas 不存在 token
- **主題 token 補齊**：iron/airtable 缺 status/cat/cal/cream（換主題切不完整）

---

## ⚠️ 注意事項
- **本批動了 ~56 個檔**（2 共用組件 + 圖示散落各頁）。其中 finance/settings 幾個檔在開工前就有未提交改動（可能別的工作），我的圖示改動疊加上去了 → **commit 時要分辨、別把別人的活也收進來**。
- **尚未 commit**（依協作紀律、等你明說）。
- 怎麼看效果：dev server 在跑（localhost:3000），登入後重整頁面就能看到 checkbox 變大、分頁漸層、編輯鈕變 Edit 圖示。

---

## 📊 進度
- 統一決策清單：**已執行 4 / ~12 項**（共用組件 2 + 圖示 4 組已動；按鈕、原生控件、Badge 病根、硬編卡片、手刻按鈕等待續）
- 完整現況：見同目錄 `UI-視覺總覽.html`、`UI-元素出處對照.md`
