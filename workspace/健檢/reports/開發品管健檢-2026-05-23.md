# 開發品管健檢 — 2026-05-23

> 對齊 CLAUDE.md「開發品管 8 維度」+ Migration SOP + Build/Commit 鐵律
> 範圍：測試覆蓋 / CI / lint / typecheck / pre-commit / audit scripts
> 對照基準：`workspace/健檢/reports/開發品管健檢.md`（3 天前、2026-05-20）

---

## 救護車式總覽

| 嚴重度 | 項目 | 影響 | vs 5/20 |
|---|---|---|---|
| 🔴 P0 | lint errors **23 個**（5/20 是 11 個）— 新增 12 個 | 紅線 F 散刻 + 紅線「迴圈內編號 RPC」 violation 蔓延 | 退步 |
| 🔴 P0 | **6 個 `no-in-loop-number-rpc` errors**（5/21 新加 rule）— finance/payments + finance/requests + stores/core | 撞 unique constraint、實際炸給 user 看 | 新增 |
| 🟠 HIGH | auth 孤兒 **1 個**（qaz1300233@gmail.com、2 天前產生、超 24h） | 占住 email、下次同信箱註冊撞唯一 | 新增 |
| 🟠 HIGH | **2 個新散刻 useSWR**（AiSidebar.tsx / MethodDialog.tsx）未進 baseline → 直接擋 CI | 紅線 F 違反、commit/PR 會被 lint error 擋 | 新增 |
| 🟠 HIGH | `audit:rls` 11 項 warning（DB skip + module_routes 9 個對不到 page.tsx + ghost capability 5 個） | L2-L5 4 層 DB 檢核完全 skip、production 偏離無人擋 | 持續 |
| 🟡 MEDIUM | `module_routes_exist`：**9 個** module route 對應 page.tsx 不存在（esim / visas / documents） | 點下去 404、HR 開了 capability 也沒用 | 新增揭露 |
| 🟡 MEDIUM | `capabilities.ts` 多出 6 個 ghost（channels.manage / finance.advance_payment.write / line_bot.*）modules/ 衍生沒有 | code 引用會誤判 / HR UI 看不到 | 新增揭露 |
| 🟡 MEDIUM | E2E full coverage 不在 CI 主路徑（slow.config 仍只本地） | 全鏈路測試靠人工 | 持續 |
| ✅ PASS | type-check 通過、tsc --noEmit 0 errors | TypeScript 守住 | 持續 |
| ✅ PASS | pre-commit hook 完整（type-check + 禁 @ts-expect-error + 禁 console.* + codegen 自動）| husky 守住 | 持續 |
| ✅ PASS | `audit:writes` 綠燈（83 trigger + 19 API insert × 30 表、0 雙寫撞車） | 紅線 E 守住 | 持續 |
| ✅ PASS | ratchet baseline 仍 145 違規（127 + 18）凍結中 | 不准新增 | 持續（但 2 個新散刻沒進 baseline、見 P0） |

**白話**：3 天內**退步**：lint errors 從 11 → 23（多 12 個）、出現 1 個 auth 孤兒、2 個新散刻 useSWR 沒走 baseline 程序就直接 commit。
最關鍵：5/21 新加的「禁迴圈內 RPC」rule 抓到 6 個 finance 寫法、是會撞 unique constraint 的真實 bug（不是 plugin 設定問題）。

---

## A. type-check 與 lint 詳細

### A1. type-check ✅
- `npm run type-check`：**0 errors**、約 2 秒（cache fresh）
- pre-commit 強制跑、`@ts-expect-error` 全禁（grep src 0 個、grep `@ts-ignore` 0 個）

### A2. lint 詳細退步分析

`npm run lint` 結果：**1794 problems（23 errors、1771 warnings）**

| 維度 | 5/20 | 5/23 | 差 |
|---|---|---|---|
| Total problems | 1526 | 1794 | +268 |
| Errors | 11 | 23 | **+12** |
| Warnings | 1515 | 1771 | +256 |

**23 個 errors 分類**：

| 類型 | 數量 | 說明 |
|---|---|---|
| `@next/next/no-img-element`（rule not found）| 13 | next.js plugin 配置問題、非 code 違反（不是真 bug） |
| `venturo/no-in-loop-number-rpc`（新 rule、5/21 加）| **6** | **真實 bug**：迴圈內 await 單筆編號 RPC、會撞 unique constraint |
| `venturo/no-direct-useswr-in-pages`（紅線 F）| **2** | **新散刻、沒走 baseline**：AiSidebar.tsx + MethodDialog.tsx |
| `@typescript-eslint/no-unused-expressions` | 2 | 真實 code 問題（AiAssistDialog.tsx 51:7 + 58:7） |

### A3. 6 個 `no-in-loop-number-rpc` 違規位置（真 bug）

```
src/app/(main)/finance/payments/_components/useBatchReceiptForm.ts:305:27  generateReceiptNo()
src/app/(main)/finance/payments/_hooks/useReceiptMutations.ts:154:35       generateReceiptNo()
src/app/(main)/finance/payments/hooks/usePaymentData.ts:83:29              generateReceiptNo()
src/app/(main)/finance/requests/_components/AddRequestDialog.submit.ts:309:27  generateRequestNo()
src/app/(main)/finance/requests/_components/AddRequestDialog.submit.ts:508:18  generateRequestNo()
src/stores/core/create-store.ts:218:31                                     generateNextCode()
```

**白話**：5/21 William 拍板新加的 ESLint rule（禁止「在 for 迴圈裡一筆一筆叫 RPC 拿編號」、要用批次 `nextReceiptNos(n)`）。
這 rule 加進去就 catch 到 6 處舊 code 違反、是會在 user 一次匯入多筆時撞 `unique constraint` 的真實 bug。
**還沒人修、也沒進 baseline**、所以 CI lint 會直接 fail。

### A4. 2 個新散刻 useSWR（沒走 baseline 程序）

```
src/app/(main)/ai/_components/AiSidebar.tsx:6:1
src/app/(main)/finance/settings/_components/MethodDialog.tsx:6:1
```

兩個都在 5/23（今天）新加進去 commit 的。違反紅線 F、應該走 entity hook 或 createReportHook。
ratchet baseline 沒收這 2 處 → commit 通過了（pre-commit type-check 過、但完整 lint 沒在 hook 跑全量）。

### A5. ESLint 自家 rules（11 條、+1 比 5/20）

| Rule | 級別 | 用途 | 違規數 |
|---|---|---|---|
| `venturo/no-direct-useswr-in-pages` | error | 紅線 F SSOT | 2 新散刻 + 18 baseline |
| `venturo/no-direct-supabase-writes` | error | 紅線 F SSOT | 0 新 + 127 baseline |
| `venturo/no-in-loop-number-rpc` | **error**（**新加**、5/21）| 防迴圈撞號 | **6 真實 bug** |
| `venturo/form-dialog-loading-required` | warn | 防連點 | warn 數混在 1771 內 |
| `venturo/no-forbidden-classes` | warn | UI 莫蘭迪色禁 | warn 數混在 1771 內 |
| `venturo/no-custom-modal` | warn | 統一 modal | warn 數混在 1771 內 |
| `venturo/button-requires-icon` | warn | 設計一致 | warn 數混在 1771 內 |
| `venturo/consistent-form-label` | warn | 設計一致 | warn 數混在 1771 內 |
| `venturo/no-hardcoded-chinese-jsx` | warn | i18n | warn 數混在 1771 內 |
| `venturo/prefer-standard-layout` | off | 未啟用 | — |

---

## B. ratchet baseline 健康度

`.eslint-suppressions.json` 現況：**145 violations / 68 files**

| Rule | Count |
|---|---|
| `venturo/no-direct-supabase-writes` | 127 |
| `venturo/no-direct-useswr-in-pages` | 18 |

**跟 5/20 數字一致、沒有 prune**（沒人跑 `npm run lint:swr-prune`）、但也沒新增進 baseline（兩個新散刻直接 fail）。

---

## C. as any / : any / suppression 統計

### C1. any 用法（grep src/）

| 模式 | 數量 |
|---|---|
| ` as any` | 15 |
| `: any\b` | 16 |
| **合計** | **31**（5/20 audit-rls 報告為 18、有上升）|

Top 5 file（any 集中）：
1. `src/app/(main)/orders/_hooks/useRoomVehicleAssignments.ts` — 5（noop placeholder hook、註解寫待重寫）
2. `src/lib/supabase/typed-client.ts` — 4
3. `src/app/(main)/orders/_components/MemberRow.tsx` — 3
4. `src/lib/swr/use-realtime-mutate.ts` — 2
5. `src/app/(main)/orders/_components/OrderStatusChangeDialog.tsx` — 2

### C2. eslint-disable 散落

`grep -rn "eslint-disable" src/` → **80 個**

By rule（top）：
- `venturo/no-forbidden-classes` suppress — 26（UI 色彩例外）
- `@typescript-eslint/no-explicit-any` suppress — 26（21 inline + 5 file-level）
- `@next/next/no-img-element` suppress — 13
- `react-hooks/exhaustive-deps` suppress — 4
- `venturo/no-direct-useswr-in-pages` suppress — 3
- `venturo/no-custom-modal` suppress — 2
- `no-console` suppress — 2
- `@typescript-eslint/no-unused-vars` suppress — 3

**5/20 → 5/23**：suppression 散刻數沒下降、但 inline `// eslint-disable-next-line` 26 個是「個案性」、非系統性問題。

### C3. console.log 散落

`grep -rn "console\.log\b" src/` → 真實散落 **3 個**（扣掉 logger / commented 後）。
pre-commit hook 守住、CI lint 守住、沒大規模新增。

### C4. --no-verify 紀錄
- `git log --grep="no-verify"`: 0 結果
- 整 `.husky/` 跟 `scripts/`：只有 `pre-push:16` 一行 fallback 提示「慎用」、沒實際使用紀錄
- ✅ William 跟 Logan 都守紀律

---

## D. Audit Scripts 跑驗結果（5/23 14:00）

### D1. `npm run audit:rls`

**結果：22 項 / 通過 11 / error 0 / warn 11**
**✅ blueprint 6 層全綠（或只剩 warning）**

但 11 個 warning 包含**重要揭露**：

#### L1 — Feature Gate
- ⚠️ `module_routes_exist`：**9 個** module route 對應 `page.tsx` 不存在：
  - `documents/[id]`、`esim`、`esim/orders`、`esim/products`、`esim/settings`
  - `library/document-types`、`library/application-service-types`、`suppliers/[code]/pricing`
  - 還有 1 條未列出
  - **業務影響**：HR 開了權限但 user 點下去 404、或者 sidebar 跳到死頁

#### L2 — Capability
- ⚠️ `capabilities_ts_synced_with_modules`：**6 個 ghost capability** 在 `capabilities.ts` 但 `modules/` 衍生沒有：
  - `channels.manage`、`finance.advance_payment.write`、`line_bot.config`、`line_bot.read`、`line_bot.write`
  - **業務影響**：code 引用 `CAPABILITIES.CHANNELS_MANAGE` 不會炸、但 HR UI 不會顯示給 admin 勾、形成「死權限」
- ⚠️ `capability_modules_in_module_tabs`：**1 個 capability module** 沒在 module-tabs.ts 定義（`line_bot`）
  - **業務影響**：同上、HR /hr/roles 找不到 line_bot 可勾、admin 沒法授權

#### L3-L5 全 skip（DB 不通）
- ❌ 本機跑沒接 production DB → 4 層 DB 檢核（scope / 狀態 / RLS / capability seed / FK）skip
- CI 也 skip（`SUPABASE_DB_URL` secret 仍未設、跟 5/20 同樣狀態）

#### L6 — 防呆 SSOT
- ✅ 中央 module 在、無 inline RPC、無 raw error.message
- ✅ as any 控制在 18 處（注：跟 grep 31 個有差、audit script 看的是 production code、grep 含 test）
- ✅ 0 個空字串 FK

### D2. `npm run audit:writes` ✅
```
✅ 沒抓到 trigger × API 雙寫撞車風險
   掃了 83 筆 trigger INSERT、19 筆 API insert、30 張表
```
紅線 E 守住、不再有 5/14 那種「trigger × API 各建 branches('HQ')」事故風險。

### D3. `npm run audit:orphans` 🔴

**抓到 1 個 auth 孤兒**：
```
qaz1300233@gmail.com  · 2 天前  · id=5d8bb5e2-6dff-415d-9913-d5074c7b0a00
```
- 年紀 > 24h、強烈建議清掉
- **業務影響**：這個 email 下次再註冊會撞 unique（auth.users 有 row 但 employees 沒有、create flow 中間 fail rollback 漏清）
- 修法：`npm run audit:orphans -- --clean` 互動式清

### D4. `npm run audit:realtime`

44 個 entity hook 訂閱的 table 列出、但**沒 `--db` 旗標、未比對 Supabase publication**。
跑全量需 `SUPABASE_DB_URL` env、跟 audit:rls 同條件。本機 / CI 都沒設。

---

## E. CI / CD 健康度（vs 5/20）

### E1. workflow 現況（**6 個、5/20 是 5 個**）

| Workflow | 觸發 | 用途 | 狀態 |
|---|---|---|---|
| `ci.yml` | push / PR | 3 jobs: quality / build / e2e-smoke | ✅ 跑、但**現在 quality 應該 fail**（23 lint errors） |
| `audit-rls.yml` | push / PR | 6 層 audit + realtime audit | ⚠️ DB secret 未設、L3-L5 skip |
| `bundle-size.yml` | PR | bundle 大小 | ✅ 跑 |
| `standards-check.yml` | PR | 10 條憲法 | ✅ 跑 |
| `line-flush-cron.yml` | cron */5min | LINE Bot | ✅ |
| **`pr-supabase-branch.yml`**（新、5/23）| PR + migrations 改動 | Supabase branch 預演 migration | ✅ 新加（Sprint A） |

### E2. CI 缺口（vs 5/20）

| 5/20 缺口 | 5/23 狀態 |
|---|---|
| `SUPABASE_DB_URL` secret 未設 | 仍未設 |
| `audit:writes` 沒在 CI | 仍未進 |
| `audit:realtime` 沒在 CI | ✅ **已進 audit-rls.yml**（code-only mode） |
| `audit:orphans` 沒在 CI | 仍未進 |
| E2E full 不在 CI | 仍只本地 slow.config |

### E3. 新增 ci 改進
- `pr-supabase-branch.yml`：5/23 William 拍板的「PR 階段先在 Supabase branch 預演 migration」、解 4/20 / 5/14 那種直打 production 的雷
- audit:realtime 進 audit-rls.yml（code-only mode、不需 DB）

---

## F. 測試覆蓋（vs 5/20）

### F1. 規模

| 類型 | 5/20 | 5/23 | 差 |
|---|---|---|---|
| 所有測試檔（spec + test）| ~73 | **93** | +20 |
| Unit tests（lib + stores + hooks + integration）| ~30 | ~35 | +5 |
| Concurrency tests | 4 | 4 | — |
| E2E specs（含 pages/security/full） | 39 | 39 | — |
| E2E full | 20 | 20 | — |
| Security e2e（cross-workspace 新加）| 1 | 1 | — |

新增測試（5/20 後新加 / modify）：
- `tests/lib/codes/nextPaymentRequestItemNumbers.test.ts`（5/21 新加批次編號 test、對應 `no-in-loop-number-rpc` rule）
- `tests/e2e/pages/finance-payments.spec.ts` / `orders.spec.ts` / `calendar.spec.ts` / `visas.spec.ts` / `dashboard.spec.ts` / `finance-requests.spec.ts`（page-level e2e）
- `tests/e2e/security/cross-workspace.spec.ts`（紅線 H 對應）

### F2. 缺口（vs 5/20、仍未補）

- **無 Realtime e2e**：channel_messages / inbox_messages 推送 → 收到鏈路無自動測試
- **無 SWR cache key 滲透 test**：紅線 G 無 e2e 驗 A 帳號登出 B 接手不能看 A 資料
- **audit:* script 本身無 test**

---

## G. Migration SOP 健康度

| 檢核項 | 狀態 |
|---|---|
| 命名 `YYYYMMDDHHMMSS_*.sql` | ✅ **832 個檔**（5/20 是 814）、3 天 +18 個 migration |
| `BEGIN; ... COMMIT;` 包圍 | ⚠️ 抽樣多數有 |
| 破壞性 migration 附反向 SQL | ⚠️ 5/13 後新檔有、舊檔多無 |
| MCP apply 紀律 | ✅ |
| `NOTIFY pgrst` reload | ✅ |
| `migrations-pending/` 草稿區 | ❌ **不存在**（5/20 已 flag、3 天沒補） |

最新 10 migration（5/21-5/23）：
- 5/23: websites_module_schema_and_seed、approval_requests_schema、sinopac_payment_provider_skeleton、block_draft_tour_financial_writes
- 5/21: cross_bank_fee + cleanup expense_categories + 6 個系列改 expense_categories

---

## H. pre-commit + check-standards（vs 5/20）

### H1. husky pre-commit hook
- ✅ TypeScript type-check
- ✅ 禁 `@ts-expect-error`（staged 守）
- ✅ `scripts/check-standards.sh --strict` 守 10 條憲法
- ✅ **新加 modules/ 變動自動跑 codegen**（5/13 後）

### H2. check-standards.sh 10 條
全條跑、跟 5/20 一致。

### H3. 強制力
- pre-commit 真的擋（exit 1）
- pre-push 有 hook、`--no-verify` 強制紀錄
- 5/20 → 5/23 git log 0 個 `--no-verify` 紀錄、紀律守住

**注**：pre-commit hook **只跑 type-check**、**不跑 `npm run lint`**。所以 2 個新散刻 useSWR 才會 commit 過 → 必須靠 CI lint 擋。

---

## I. 修法優先級（William 拍板用）

### P0（業務語言：今天就會炸的）
1. **修 6 個 `no-in-loop-number-rpc` 真實 bug**（finance/payments + finance/requests + stores/core）
   - 業務影響：user 一次匯入多筆收款 / 請款、會撞 unique constraint、轉成「無法存檔」
   - 解法：改用批次 wrapper（已有 `nextPaymentRequestItemNumbers` 範例）
2. **清 1 個 auth 孤兒**（qaz1300233@gmail.com）
   - `npm run audit:orphans -- --clean` 一鍵清
3. **修 2 個新散刻 useSWR**（AiSidebar.tsx + MethodDialog.tsx）
   - 改走 entity hook 或加進 baseline（先讓 lint 過）

### P1（業務語言：架構黑洞、未來會炸）
4. **修 9 個 module route 對不到 page.tsx**（esim / visas / documents）
   - HR 開了權限但點下去 404、UX 爛
5. **清 6 個 ghost capability**（channels.manage / line_bot.* / advance_payment.write）
   - 要嘛從 capabilities.ts 砍掉、要嘛在 modules/ 補上對應宣告
6. **修 2 個 `no-unused-expressions` 真 bug**（AiAssistDialog.tsx 51:7、58:7）
7. **修 11 個 `@next/next/no-img-element` rule 設定問題**（5/20 已 flag、3 天沒修）

### P2（基礎設施債）
8. CI 設 `SUPABASE_DB_URL` secret → audit:rls L3-L5 + audit:realtime 全量
9. `audit:writes` / `audit:orphans` 加進 audit-rls.yml workflow
10. 補 Realtime e2e + SWR cache key 滲透 e2e
11. 建 `migrations-pending/` 草稿區（5/20 已 flag）

### P3（漸進）
12. `form-dialog-loading-required` 升 error（先 ratchet）
13. `pre-commit` 補跑 `npm run lint`（防新散刻直接 commit）

---

## J. 跟 5/20 的差異總結

**進步**：
- ✅ 新增 `audit:realtime` 進 CI workflow
- ✅ 新加 `pr-supabase-branch.yml` workflow（migration PR 預演）
- ✅ 新加 `venturo/no-in-loop-number-rpc` ESLint rule（5/21 William 拍板）→ catch 出 6 個真實 bug
- ✅ 新增 page-level e2e（finance-payments / orders / calendar / visas / dashboard / finance-requests）
- ✅ 新增 cross-workspace security e2e

**退步**：
- ❌ lint errors **11 → 23**（多 12 個）
- ❌ 2 個新散刻 useSWR 直接 commit、ratchet 程序沒守
- ❌ auth 孤兒首次出現（1 個、超 24h）
- ❌ `as any` / `: any` 31 個（5/20 audit-rls 報 18、有上升）

**持續未動**：
- ⚠️ CI `SUPABASE_DB_URL` secret 仍未設
- ⚠️ `audit:writes` / `audit:orphans` 仍未進 CI
- ⚠️ migrations-pending/ 草稿區仍不存在
- ⚠️ E2E full 仍只本地 slow.config

---

## K. 結論（給 William 業務語言）

**整體**：開發品管 3 天內**有進有退**、淨退步。

**好消息**：
- 5/21 加的「禁迴圈內編號 RPC」rule 抓到 6 個會撞號的真實 bug、是新 rule 工作（沒有 rule、這些 bug 就會默默炸給客戶看）
- 新加 PR-time Supabase branch 預演、未來動 schema 不會直接打 production
- 測試多了 20 個檔（page-level e2e + security e2e）

**壞消息**：
- 6 個迴圈撞號 bug 還沒修、CI lint 會 fail
- 2 個新散刻 useSWR 沒走 ratchet、直接 commit 過了（pre-commit 沒跑 lint）
- 1 個 auth 孤兒、3 天沒人發現
- L3-L5 RLS DB 檢核 4 個層全 skip、production 偏離無人擋

**最快 ROI**：
1. 跑 `npm run audit:orphans -- --clean` 清 auth 孤兒（30 秒）
2. 修 6 個迴圈撞號 bug（2-3 小時、有 `nextPaymentRequestItemNumbers` 範例可仿）
3. CI 設 `SUPABASE_DB_URL` secret（30 分鐘、解 L3-L5 全 skip）
