# 夜間 audit 彙整 — 2026-05-26（loop A5）

> 由夜間自動優化 loop 跑、code-grep fallback（Mac 本機 DB 不通，DB 層檢核自動 skip；CI Linux 會跑全套）。
> 純分析、無 code 變更。對應主線：`2026-05-26-讀取快取同步-全盤盤點.md`。

## 結果總表

| audit | 結果 | 重點 |
|---|---|---|
| `audit:rls`（6 層 blueprint）| ✅ **0 error / 11 warn** | 11 warn 全是「DB 不通、本層 skip」（L5 RLS、audit_fk_employees、entity_hook_schema_drift）。**code 層全綠**：中央 module 在、0 inline 編號 RPC、0 raw error.message、0 空字串 FK、as any 控制在 17 處、3 個已砍欄位 0 殘留。 |
| `audit:writes`（trigger×API 雙寫）| ✅ **無撞車** | 掃 83 trigger INSERT / 19 API insert / 30 表，無 trigger×API 同表雙寫風險。 |
| `audit:realtime`（entity hook 訂閱）| ⚠️ code-grep only | 列出 entity hook 訂閱的表；**publication 差集要 `-- --db` 才驗**。盤點 doc 已用 agent 確認 `brands`/`checks` 訂了但沒進 publication（migration 已寫未 apply）。 |
| `audit:stale-refs` | ✅ 全綠 | validator 引用都指向存在檔案。 |

## 解讀
- **本機能驗的 code 層全部綠**，沒有新的 blueprint 偏離。今天 A 層的改動（footgun×8 + 2 條 lint 守門）沒有引入 audit 違規。
- **DB 層檢核（RLS policy、FK 指向、schema drift、realtime publication 差集）本機驗不了**，需在 CI（有 `SUPABASE_DB_URL`）或 William 用 MCP 跑。最關鍵的待驗項：
  1. `brands`/`checks` realtime publication（migration `20260526120000` 待 apply）。
  2. entity hook schema drift（select 欄位 vs 實際 column）。
- `audit:realtime` 腳本本身有個已知盲點（見盤點 doc §2）：只掃 `createEntityHook` 的表，**看不到 6 個自訂 channel**（tours-paginated / calendar / order_members / 2× AI broadcast / presence），所以它的「published 卻沒人用」會誤判。→ 列進 B/C 層待修（A 不動腳本）。

## 給 William 早上的動作（DB 層、需你跑）
- `npm run audit:realtime -- --db`（設 `SUPABASE_DB_URL`）確認 publication 差集。
- 或用 MCP `execute_sql` 跑盤點 doc §2 的 publication 查詢。
- apply `20260526120000_add_brands_checks_to_realtime_publication.sql`（補 brands/checks）。
