# SWR 水管健檢報告 — 2026-05-19

> 作者：Max（資安 / 效能 / 權限 engineer）
> 觸發：5/19 修「景點不能刪」+「供應商存不了檔」期間、發現 useTourEdit 散刻 raw `mutate('字串 key')` 全敲空氣、是「user 改完看不到變化、要 F5」的根本病根之一。
> William 拍板：不打游擊、做整間公司水管健檢、迭代到全綠。

---

## 救護車式總覽（先講會死人嗎）

| 嚴重度 | 項目 | 影響 |
|---|---|---|
| 🔴 CRITICAL | 9 處散刻 `mutate('字串')`、**100% 敲空氣** | 收款 / 銷帳 / 團統計 / 訂單改完、UI 全卡 stale 要 F5 |
| 🔴 CRITICAL | 3 個 Critical 寫入黑洞（CheckDialog / PNR match save / useTourEdit 級聯） | 建立支票看不到、PNR 匹配後成員不更新、團名改完訂單列表 stale |
| 🔴 CRITICAL | 4 個 entity 訂 realtime 但 Supabase publication 沒加（todos / cis_clients / cis_pricing_items / cis_visits） | 訂閱白訂、Postgres 不推播、永遠 stale |
| 🟠 HIGH | 23 個高頻 DB 表沒 entity hook（order_members 125 處呼叫最痛） | SSOT 破碎、新功能各自寫各自的 SWR |
| 🟠 HIGH | `apiMutate` **整個 codebase 0 個 caller**（紅線 #F SSOT 沒落地） | 抽象層蓋好但沒人用 |
| 🟠 MEDIUM | 21 處頁面 / hook 直接 `useSWR`（A=4 / B=12 / C=5） | A 類 4 處該搬、C 類 5 處該補新 entity hook |
| 🟠 MEDIUM | 11 處 client 端散刻寫入、5 處沒 invalidate | 寫了 cache 不通知、UI 沒變化 |
| 🟠 MEDIUM | 4 個財務報表沒走 `createReportHook`（DisbursementTab / IncomeTab / OverviewTab / TourPnlTab） | 跨表聚合邏輯留在 UI、SSOT 破碎 |
| 🟡 LOW | 3 個 entity 缺 `invalidate` export（cities / regions / workspace-bonus-defaults） | 沒寫操作、暫無影響 |
| ✅ PASS | per-user cache key 防污染（紅線 #G） | 滿分綠燈、登入 / 登出 / 版本控制都對齊 |

**白話總結**：自來水廠（抽象層）蓋好了、但全公司還在挑井水（散刻 raw mutate / 直接 useSWR / 直接 supabase.from().update()）。最痛的 9 處連桶子都拿錯、挑了倒進空地。

---

## A1. 抽象層覆蓋率

42 個 entity 檔對應約 40 張 DB 表、整體覆蓋率約 65%（以 caller 出現頻率計）。

**有完整 entity hook 的核心表**：
- orders / receipts / payment_requests / payment_request_items / disbursement_orders ✅
- tours / itineraries / quotes ✅
- employees（只 read、無 CRUD）

**🔴 缺口表（高頻使用但無 entity hook）**：

| 表名 | caller 數 | 用途 | 嚴重度 |
|---|---|---|---|
| order_members | **125** | 團員異動、影響 paid_amount / total_participants | 🔴 CRITICAL |
| checks | 51 | 支票管理 | 🟠 HIGH |
| journal_vouchers | 48 | 帳務憑證、核心財務表 | 🟠 HIGH |
| inbox_conversations | 44 | 對話總覽、跨通路 | 🟠 HIGH |
| contracts | 34 | 合約管理 | 🟠 MEDIUM |
| bonus_pending | 31 | 獎金待結算、薪資 | 🟠 MEDIUM |
| journal_lines | 28 | 帳務行項 | 🟠 MEDIUM |
| inbox_messages | 27 | 訊息明細 | 🟠 MEDIUM |
| salary_settlements | 23 | 月度薪資結算 | 🟠 MEDIUM |
| chart_of_accounts | ? | 會計科目 | 🟠 MEDIUM |

---

## A2. 散刻 raw `mutate('xxx')` 全定位（9 處、100% 敲空氣）

`createEntityHook` 真實 cache key 是 `entity:${tableName}:list:v${hash}` / `entity:${tableName}:detail:${id}` / `entity:${tableName}:paginated:${JSON.stringify(params)}` / `entity:${tableName}:slim`、**全部不是純表名**。

| 檔案:行 | 散刻 key | entity 真實 key | 命中? |
|---|---|---|---|
| `tours/_services/tour-stats.service.ts:74` | `'tours'` | `entity:tours:list:v...` | ❌ |
| `tours/_services/tour-stats.service.ts:75` | `'tour-${id}'` | `entity:tours:detail:{id}` | ❌ |
| `tours/_services/tour-stats.service.ts:134` | `'tours'` | 同上 | ❌ |
| `tours/_services/tour-stats.service.ts:135` | `'tour-${id}'` | 同上 | ❌ |
| `tours/_hooks/useToursPaginated.ts:195` | `'tours'` | 同上 | ❌ |
| `tours/_hooks/useToursPaginated.ts:214` | `'tours'` | 同上 | ❌ |
| `finance/payments/_services/receipt-core.service.ts:196` | 3 個 (`'tours'` / `'orders'` / `'receipts'`) | 各自 entity key | ❌ × 3 |
| `finance/payments/_services/expense-core.service.ts:95` | `'tours'` | 同上 | ❌ |
| `orders/_services/order-stats.service.ts:80+` | `'orders'` | 同上 | ❌ |
| `tours/_hooks/useTourEdit.ts` ← 5/19 已修 | — | — | ✅ 改 invalidateXxx |

**🚨 smoking gun**：
- **`receipt-core.service.ts:196`** — 收款核心、敲 3 個空氣 key。「收款後 tours / orders / receipts UI 全卡 stale」直接病根。
- **`tour-stats.service.ts` 4 處** — 團統計重算後敲空氣、待結案計數永遠錯。
- **`expense-core.service.ts:95`** — 支付完成不刷 tours、團成本卡舊。

---

## A3. invalidate helper export 缺口

| Entity 檔 | 狀態 | 備註 |
|---|---|---|
| itineraries.ts | ✅ 已修 (5/19) | 原本是私有 `_invalidateItineraries`、改 export |
| employees.ts | ❌ 無 invalidate | 只 read、無寫操作、可不補 |
| finance-summary.ts | ❌ 無 invalidate | RPC 讀取、無寫操作 |
| notes.ts | ❌ 空檔 | dead code、可刪 |
| cities.ts | ⚠️ 缺 CRUD + invalidate | 只 read、未來寫操作要補 |
| regions.ts | ⚠️ 缺 CRUD + invalidate | 同上 |
| workspace-bonus-defaults.ts | ⚠️ 缺 CRUD + invalidate | 同上 |

---

## B3. 頁面 / hook 直接 useSWR（21 處）

**分類規則**：
- **A 類**：對應 DB 表有 entity hook、作者沒用、該搬
- **B 類**：邏輯複雜（跨表 join / 自訂 RPC / 樂觀更新 + realtime）、保留並加 comment 說明
- **C 類**：DB 表存在但無 entity hook、該補

### A 類 — 該搬（4 處）

| 檔案 | 該改用 |
|---|---|
| `tours/[code]/page.tsx` | `useTour(code)` |
| `tours/_hooks/useTourItineraryItems.ts` | `useItineraryItems()`（已有） |
| `accounting/accounts/components/EditAccountDialog.tsx` | `useChartOfAccounts()`（待補） |
| `orders/_hooks/useMemberEditDialog.ts` | `useOrderMembers()`（待補） |

### B 類 — 保留 + 加 comment（12 處）

| 檔案 | 保留理由 |
|---|---|
| `settings/company/_components/BranchesSection.tsx` | 自訂 API route（/api/organization/branches） |
| `tours/_components/ProfitTab.tsx` | 跨 receipt + order + payment_requests + bonus 計算 |
| `tours/_hooks/useToursPaginated.ts` | 分頁 + 樂觀更新 + realtime、createEntityHook 不支援分頁參數 |
| `library/attractions/_components/tabs/RegionsTab.tsx` | workspace 側邊欄快取、跨 table join |
| `workspaces/[id]/_components/ai-health-tab.tsx` | 自訂 API route（/api/ai/health）|
| `workspaces/[id]/_components/QuotaHistorySection.tsx` | 複雜 RPC 彙總 |
| `ai/_components/AiRetrospectiveTab.tsx` | 自訂 API route（/api/ai/rag-topics）|
| `ai/_components/AiConversationsTab.tsx` | 跨 conversations / messages / memory / notes / postback_templates |
| `bot/[lineUserId]/_components/CustomerInfoSidebar.tsx` | 客戶訂單聚合 |
| `ai/_components/AiSettingsTab.tsx` | LINE 集成複雜配置 RPC |
| `library/archive-management/page.tsx` | 歸檔級聯（tours + itinerary + calendar）|
| `dashboard/_services/dashboard.service.ts` | 服務層聚合 |
| `finance/requests/_components/AddRequestDialog.edit-ops.ts` | 批次操作 + 樂觀更新 |

### C 類 — 該補 entity hook（5 處）

| 檔案 | 缺的 entity hook |
|---|---|
| `shared-data/countries/page.tsx` | `useCountriesRef()` （ref_countries 表）|
| `shared-data/banks/page.tsx` | `useBanksRef()` （ref_banks 表）|
| `shared-data/airports/page.tsx` | `useAirportsRef()` + `useCountriesRef()` |
| `components/bank-combobox.tsx` | `useBanksRef()`（共用元件）|

---

## B4. Client 端散刻寫入路徑（11 處）

### 🔴 Critical — 沒 invalidate 黑洞（3 處）

| 檔案:行 | 寫哪張表 | 動作 | 結果 |
|---|---|---|---|
| `accounting/checks/components/CreateCheckDialog.tsx:77` | checks | insert | 建支票看不到、user 以為失敗可能重複建 |
| `orders/_components/usePnrMatchSave.ts:155 / 180 / 246` | order_members + member_flights | 3 個 insert/update | PNR 匹配後成員清單永遠舊資料、新成員不顯示 |
| `library/archive-management/page.tsx` | tours + tour_itinerary_items + calendar_events | 級聯 update+delete | 歸檔後 3 處全 stale |

### 🟠 待補（5 處）

| 檔案:行 | 動作 | 修法 |
|---|---|---|
| `accounting/accounts/components/EditAccountDialog.tsx` | delete chart_of_accounts | 補 invalidate / 改 entity hook |
| `tours/_hooks/useTourCreateOperation.ts:223` | insert tour_role_assignments | 改 entity hook / apiMutate |
| `tours/_hooks/useTourEdit.ts:229,232` | update orders + receipts（5/19 已部分修）| 確認所有級聯 invalidate 都到位 |

### ✅ Service 層可接受（3 處）

| 檔案 | 備註 |
|---|---|
| `dashboard.service.ts:60,63,65` | 服務層、應後補 invalidate at caller |
| `tour_dependency.service.ts` | 服務層批次刪、caller 應 invalidate |
| `payment-request-items.service.ts:19` | 服務層、caller invalidate |

---

## C5. Realtime 訂閱缺口

`createEntityHook` 內建會自動掛 `useRealtimeSync` 訂 Supabase channel、**但前提是 Supabase 端 `supabase_realtime` publication 要有那張表**。

| Entity | 訂 realtime | publication | 結果 |
|---|---|---|---|
| **todos** | ✅ 明確標 `cache: high, // Realtime 刷` | ❌ 缺 | 🔴 訂了白訂 |
| **cis_clients** | ✅ createEntityHook 內建 | ❌ 缺 | 🔴 訂了白訂 |
| **cis_pricing_items** | ✅ 內建 | ❌ 缺 | 🔴 訂了白訂 |
| **cis_visits** | ✅ 內建 | ❌ 缺 | 🔴 訂了白訂 |
| channel_messages | ✅ | ✅ | 正常 |
| 其他 33 tables | ❌ 無 realtime | ✅ 34/34 | 單向（無 push、合理）|

**修法**：對這 4 張表跑：
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE public.todos;
ALTER PUBLICATION supabase_realtime ADD TABLE public.cis_clients;
ALTER PUBLICATION supabase_realtime ADD TABLE public.cis_pricing_items;
ALTER PUBLICATION supabase_realtime ADD TABLE public.cis_visits;
```

**自動偵測**：`scripts/audit-realtime.ts` 已就位、加 `--db` 旗標跑可自動比對。應加進 CI / 動 entity 前 pre-commit。

---

## C7. createReportHook 採用率

### 已採用（5 個報表）✅
- `useReceivables` / `usePayables` / `useSalesPerformance` / `useUnclosedTours` / `useBankBalances`

### 漏網（4 個 Tab、應升格）

| Tab | 現狀 | 問題 |
|---|---|---|
| **DisbursementTab** | 直接 `usePaymentRequests + useDisbursementOrders` + 手動 useMemo | 聚合邏輯留在 UI、不可重用、無 cache |
| **IncomeTab** | 直接 `useReceipts` + 手動篩選日期 / 分類統計 | 同上 |
| **OverviewTab** | 直接 `useReceipts + usePaymentRequests` + 多維分組 | 同上 |
| **TourPnlTab** | placeholder「敬請期待」 | 開發中、未來走 createReportHook |

---

## C8. per-user cache key 防污染（✅ 滿分）

| 檢查項 | 狀態 |
|---|---|
| `getCurrentCacheKey()` — user_id 後綴 | ✅ |
| SWR provider 用 `CACHE_STORAGE_KEY` | ✅ |
| `localStorageProvider()` 不散刻 | ✅ |
| 登入後 `clearAllSwrCacheKeys()` | ✅ `auth-store.ts:206` |
| 登出時 `clearAllSwrCacheKeys()` | ✅ `auth-store.ts:137` |
| 版本控制（v1 → v2、5/17 bump）| ✅ |
| 無散刻 `localStorage.setItem('venturo-swr-cache-*')` | ✅ |
| Server-side logout 雙保險 | ✅ |
| 同台多帳號交互安全 | ✅ |

紅線 #G 完整守住、跨帳號污染風險為零。**無修復項**。

---

## 修法排程（P0 → P2）

### P0 — 現在做、user 今天有感（半天）

1. **9 處散刻 `mutate('字串')` 全改 `invalidateXxx()`**（A2）
   - `tour-stats.service.ts` × 4
   - `useToursPaginated.ts` × 2
   - `receipt-core.service.ts` × 3 key
   - `expense-core.service.ts` × 1
   - `order-stats.service.ts` × 1

2. **3 個 Critical 寫入黑洞補 invalidate**（B4）
   - `CreateCheckDialog.tsx:77` — 補 `invalidateChecks()`（先 raw、待 checks entity 補）
   - `usePnrMatchSave.ts:155/180/246` — 補 `invalidateOrderMembers()` + `invalidateMemberFlights()`（待 entity 補時對齊）
   - `library/archive-management/page.tsx` — 補級聯 invalidate

3. **4 個 entity 補 realtime publication**（C5）
   - SQL `ALTER PUBLICATION supabase_realtime ADD TABLE` × 4

### P1 — 1-2 天、補抽象層缺口

4. **補 5 個 entity hook**（B3 C 類 + A2 缺口）
   - `useBanksRef` / `useCountriesRef` / `useAirportsRef` （ref_* 表）
   - `useChartOfAccounts` （accounting）
   - `useOrderMembers` （125 處 caller、最高 ROI）

5. **A 類 4 處 useSWR 搬到 entity hook**

6. **B 類 12 處加保留 comment**（標清楚為什麼）

7. **4 個報表 Tab 升格 `createReportHook`**

### P2 — 半天、防再犯

8. **ESLint rule**（紅線 #F Phase 3）
   - `venturo/no-direct-useswr-in-pages` — page / component 直接 useSWR → error
   - `venturo/no-string-mutate-keys` — `mutate('xxx')` 字串 literal → error
   - `venturo/require-invalidate-after-mutation` — `supabase.from().update/insert/delete` 後沒 invalidate → warning

9. **新 audit script**
   - `npm run audit:swr-keys` — 掃 raw `mutate('字串')` 對 entity registry 比對
   - 把 `audit:realtime` 加進 CI（PR 自動跑）

10. **Phase 3：採用 `apiMutate`**
    - 整個 codebase 0 caller、紅線 #F SSOT 沒落地
    - 選 3-5 個高頻寫入 route（tours update / orders update / receipts confirm）改成它做 case study
    - 寫成 SOP 推給未來新功能

### P3 — 補 entity hook 大遷移（背景持續）

按 A1 缺口表優先級補 entity：
- `order_members` (125) → `journal_vouchers` (48) → `inbox_conversations` (44) → ... → `salary_settlements` (23)

---

## 工程紀律補丁

**動 entity hook 前必跑**：
```bash
npm run audit:rls          # 既有
npm run audit:writes       # 既有
npm run audit:realtime --db # 既有、加 --db 旗標查 publication
npm run audit:swr-keys     # 新加（P2）
```

**動完必複查**：複查機制 = 用本報告當 checklist 重掃、找漏項 → 補 → 再掃、直到全部 ✅。
