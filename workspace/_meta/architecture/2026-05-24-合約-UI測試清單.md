# 合約管理 — UI 測試清單（逐筆親眼確認、給 openclaw 走查用）

> 2026-05-24。William 要求：不要 AI 自我認證、要親眼看每一筆 UI 過。用 openclaw 可視瀏覽器跑、逐筆打勾。
> 環境已備：STANDARD 租戶已開 tours.contract feature + 該帳號「系統主管」角色有 tours.contract.read/write。
> 測試帳號：公司代號 `STANDARD` / Email `standard@gmail.com` / 密碼 `00000000`
> 測試環境：dev server（localhost:3000）或 production（erp.venturo.tw、已部署）

## A. 登入 + 進入
- [ ] A1 登入頁填 公司代號 STANDARD + Email + 密碼 → 點登入 → 成功進入（沒被擋、看到 sidebar）
- [ ] A2 sidebar 看得到「訂單」入口

## B. 合約按鈕（守門驗證）
- [ ] B1 進「訂單」列表 → 列表正常載入、看到訂單
- [ ] B2 訂單那一排**看得到「合約」按鈕**（因 STANDARD 已開 tours.contract + 有權限）
- [ ] B3（反向）若關掉 STANDARD 的 tours.contract → 合約按鈕應**消失**（守門有效）

## C. 建立合約（核心流程）
- [ ] C1 點「合約」按鈕 → Dialog 跳出（該訂單無合約 = 建立模式）
- [ ] C2 「簽約對象」可切「個人 / 公司行號」
- [ ] C3 「代表人」下拉選一位團員 → **姓名、身分證自動帶入**（驗自動帶）
- [ ] C4 「集合時間」：若該團有航班 → **自動帶「起飛前 3 小時」**；地點預設「桃園國際機場第一航廈」（沒航班=空、可手填）
- [ ] C5 填簽署人電話、附件 toggle（行程表 / 團員名單）
- [ ] C6 點「建立合約」→ 出現成功 toast、Dialog 變成「狀態檢視」

## D. 合約狀態 + 動作
- [ ] D1 狀態檢視顯示：合約編號 + 簽約人 + 狀態（草稿）
- [ ] D2 點「列印合約」→ 開新分頁顯示 PDF
- [ ] D3 **PDF 上集合時間/地點要印對**（= 驗整合 bug B 的修正：gather* 欄位有接上）
- [ ] D4 點「複製簽約連結」→ toast「已複製」
- [ ] D5 點「開啟簽署頁」→ 開客戶端簽署頁、4 步流程正常顯示

## E. 總覽簽約進度
- [ ] E1 回該團「總覽」→ 財務列看到第 5 欄「簽約進度」
- [ ] E2 數字正確（剛建是草稿未簽 → 顯示「已簽 0 / 共 N」、未全簽=金色不轉綠）
- [ ] E3 把該合約走完客戶簽署 → 總覽進度 +1（已簽數增加）

## F. 重開驗證（資料有掛對訂單 = 驗 bug A）
- [ ] F1 關閉 Dialog、重新點同一訂單的「合約」→ 應顯示**剛建的那張合約的狀態檢視**（不是又跳建立表單）→ 證明 order_id 有掛對

---

## ⚠️ 已知未做（不在本次測試範圍、別當 bug）
- 整團合約模式（公司一份、order_id null）— 需改 API
- 取消 / 重新產生合約 — 需確認 API

## 測完
- 哪幾筆過 / 哪幾筆掛 → 回報，掛的我修。
- 全過 = 合約 per-order 流程真的 UI 驗證通過（不是 AI 自我認證）。
