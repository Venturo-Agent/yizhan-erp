# 合約管理系統 — 自主建置進度（William 睡覺中、醒來看這份）

> 2026-05-23 22:00 起 主 Claude 自主建置。分支 `feat/contract-management`。
> 設計依據：`2026-05-23-合約管理系統-設計提案.md`。
> 原則：走分支不推 prod、全程 type-check/lint/audit 綠、卡到不可逆決策留註記。

---

## 現況確認（建置前查證）

- ✅ 後端成熟：`contracts` 表（tour_id 必 + order_id 選 + 完整簽署存證）、API（create/list/sign/paper-sign/pdf）、客戶端簽署流程 `public/contract/sign/[code]`
- ✅ capability 已存在：`tours.contract.read` / `tours.contract.write`（你點名的「HR 權限」已有、不用新建）
- ✅ feature gate：合約歸 `tours` feature 旗下（module-tab `contract`、category premium）
- ⚠️ 沒有現成的內部合約管理 Dialog（舊 TourContractTab = stub）→ 要建
- ⚠️ `orders/_contracts/` 只有 labels → 訂單端 UI 要建

## 待 William 醒來拍板（不可逆 / 需決策、我先不亂猜）

- [ ] 合約 feature 要不要從 tours 獨立成自己的 feature flag？（目前傾向沿用 tours feature + tours.contract capability、不過度切分。若要獨立成 addon、醒來說）
- [ ] 集合時間/航廈用 contract_data jsonb 存（已決定、低風險）

## 建置進度（逐項勾、附 commit）

- [x] P1 資料層：contracts entity hook（讀 by tour/order）— `src/data/entities/contracts.ts` + 註冊 index、type-check 綠
- [x] P2 總覽簽約進度（雙模式：整團 / 每訂單 X/Y）— tour-overview.tsx 加第 5 欄、type-check+lint 綠
- [x] P3 訂單「合約」按鈕 + 管理 Dialog — OrderContractDialog（建立/狀態/列印 PDF/複製簽署連結/開啟簽署頁）+ SimpleOrderTable 按鈕 + OrderListView 串接。type-check+lint(單檔)綠。reuse 既有 create/pdf API + 客戶簽署流程
- [ ] P4 流程自動帶（代表人下拉帶資料 / 航班算集合時間 / 航廈+聯絡人）
- [ ] P5 5 SSOT + 6 層確認 + seed（feature/capability 給對的 role）
- [ ] P6 全檢查綠（type-check / lint / audit:rls / audit:writes / check-standards）

## 檢查結果（最後填）

（待建置中更新）

---

*主 Claude 自主建置中、即時更新此檔。*
