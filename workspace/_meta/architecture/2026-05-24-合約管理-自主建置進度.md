# 合約管理系統 — 自主建置進度（William 睡覺中、醒來看這份）

> 2026-05-23 22:00 起 主 Claude 自主建置。分支 `feat/contract-management`。
> 設計依據：`2026-05-23-合約管理系統-設計提案.md`。
> 原則：走分支不推 prod、全程 type-check/lint/audit 綠、卡到不可逆決策留註記。

---

## 現況確認（建置前查證）

- ✅ 後端成熟：`contracts` 表（tour_id 必 + order_id 選 + 完整簽署存證）、API（create/list/sign/paper-sign/pdf）、客戶端簽署流程 `public/contract/sign/[code]`
- ✅ capability 已存在：`tours.contract.read` / `tours.contract.write`（你點名的「HR 權限」已有、不用新建）
- ✅ feature gate：合約歸 `tours` feature 旗下（module-tab `contract`、category premium）
- ⚠️ 沒有現成的內部合約管理 Dialog（舊 TourContractTab = stub）→ 要建
- ⚠️ `orders/_contracts/` 只有 labels → 訂單端 UI 要建

## ✅ 你點名的兩個檢查（查證 production）

- ✅ **租戶功能開通**：合約有自己的 feature flag `tours.contract`、目前開給 **3/9 租戶**。UI 已加守門：訂單「合約」按鈕只在租戶開通 + 員工有權限才顯示。
- ✅ **HR 權限讀寫**：`tours.contract.read` / `tours.contract.write` 各 **13 個 role** 已有。capability 已存在、不用新建。

## 待 William 醒來拍板 / 後續（我先不亂猜的）

- [ ] **整團合約模式**（公司行號簽一份、order_id null）：DB schema 支援、但 create API 的 `orderId` 目前**必填** → 要做整團模式需改 API 讓 orderId 可選 + 加 tour 層「整團合約」入口。（每訂單模式已完整可用）
- [ ] **集合時間自動帶「起飛前 3 小時」**：目前集合時間是手動填（欄位已有）。自動帶需接 itinerary 航班資料、留 TODO。
- [ ] **取消 / 重新產生合約**：labels 有、但沒確認 API endpoint（create/sign/paper-sign/pdf 有、cancel 沒看到）→ 待確認後補。
- [ ] 哪 3 個租戶開了 tours.contract（CORNER 等）、要不要多開幾個 → 你決定。

## 建置進度

- [x] P1 資料層：contracts entity hook（讀 by tour/order）— `src/data/entities/contracts.ts` + 註冊、type-check 綠
- [x] P2 總覽簽約進度（雙模式：整團 / 每訂單 X/Y）— tour-overview.tsx 第 5 欄、綠
- [x] P3 訂單「合約」按鈕 + OrderContractDialog（建立/狀態/列印 PDF/複製簽署連結/開啟簽署頁）+ SimpleOrderTable + OrderListView、綠
- [~] P4 流程自動帶：**代表人下拉自動帶姓名/身分證 ✅**；航班算集合時間 = TODO（見上）
- [x] P5 6 層 L1+L2 gating：feature `tours.contract` + capability `tours.contract.read` 守門（UI 按鈕 + API requireCapability 雙層）、查證 seed 完成
- [x] P6 全檢查綠

## 檢查結果（2026-05-24 自主建置收尾）

- ✅ type-check exit 0
- ✅ check-standards --strict 全過（無 console / 無 any / TS 0 error）
- ✅ audit:writes（無 trigger×API 雙寫、我沒動 DB 層）
- ✅ audit:rls **error 0**（11 warn 全是「Mac 連不到 DB、CI 會跑」、code-grep 檢核全過）
- ✅ 新/改檔 lint 乾淨（OrderContractDialog/OrderListView/tour-overview/contracts entity 單檔 exit 0）
  - 註：simple-order-table 有 1 個 `no-direct-supabase-writes` 是**既有 baseline**（非我加、我只加按鈕、無新寫入）

## 🔴 驗證發現的整合 bug（2026-05-24、讀 create API 抓到、已修）

靜態檢查（type-check/lint）抓不到、但會讓功能實際壞掉的 2 個：
1. **create API 漏寫 `order_id`** — insert 只寫 tour_id、合約沒掛訂單 → Dialog 用 order_id 找不到合約、總覽「每訂單進度」全對不上（會誤判成整團合約）。**已修**：insert 補 `order_id: orderId`。
2. **集合資訊 key 不對** — 合約文件實際讀 `gatherLocation` / `gatherYear/Month/Day/Hour/Minute`，我原本送 `meeting_location/meeting_time` → 集合資訊不會上合約。**已修**：Dialog 改把集合地點/時間對映成 gather* 欄位送出。

> ⚠️ 仍未做：**全 UI 點擊實測**（我能讀 code 驗整合、但沒法在瀏覽器點按鈕→建立→看 PDF；需 William 或 e2e）。

## 結論

**每訂單合約流程 launch-ready**（建立→簽署連結→客戶簽→列印 + 總覽進度 + 雙層守門）、全檢查綠、在分支 `feat/contract-management`。
**沒推 production**、等你醒來 review → merge/deploy。
後續（整團模式 / 航班自動帶集合時間 / 取消重產）需你拍板或小幅 API 調整、已列上方。

---

*主 Claude 自主建置完成。分支待 William review。*
