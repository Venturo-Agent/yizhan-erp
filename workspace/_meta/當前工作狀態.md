# 當前工作狀態（活文件 — 持續讀取+複寫、撐過 context 壓縮）

> **用途**：長期工作的續航錨點。Claude 壓縮/新 session 後**先讀這份**就知道進度、接續做。也是對外單一入口。
> 規則：常讀常寫、保持精簡、過期就改寫。最後更新：2026-05-24。

---

## 0. 我們是誰 / 標準（指標）
- 系統：一棧 ERP — 多租戶 → 多產業 SaaS ERP。
- master 規格：`workspace/_meta/系統規格書-v1.md`
- 100 分標準：資安#1 / 效能#2(不全撈不浪費) / SSOT#3 / 完整統一 / 強迫症乾淨。**完整 > 避險**（沒 prod user）。唯一不打折=正確性。
- 工作模式：**Claude 主導架構+QC、OpenCLAW 寫手、William 拍商業**。memory: feedback_claude_lead_architect_openclaw_writer。
  - 派 OpenCLAW：`openclaw agent --agent main --timeout <s> --message "..."`（背景跑、強措辭 no-commit）。已驗證能跑 repo 指令 + 守 no-commit。
  - 回報 William：**業務語言**、不丟技術細節（他看不懂）。**每則走 Telegram reply 工具**。

## ⏸ 新對話先看這裡：現況 + 下一步（2026-05-24 已 push 上線）

> **本支線已 merge 進 main + push 部署**（A 階段 + B 地基一起上）。Coolify 自動部署到 erp.venturo.tw。

**✅ 收款頁接線完成（2026-05-24、type-check + lint + production build 全綠、`/finance/payments` 路由編入）**
- page.tsx 已從 `usePaymentData` 全撈改成 `useReceiptsListView`(伺服器分頁) + `ListPageLayout` serverPagination + onSort/onSearchChange server 化 + 「只看未付」headerActions Select。
- 新建 `_hooks/useReceiptListActions.ts`（列動作核准/編輯/刪除 + optimistic、refresh 吃 page 的 refreshAll）。**注意跟既有 `useReceiptMutations`（建單用）不同責任、別混淆**。
- 難點解法：`refreshAll = invalidateReceipts()(entity、給 tours/treasury/reports 共用) + listView.refresh()(本頁分頁 key)` 兩個都刷。
- 防白閃：`loading={loading && receipts.length===0}` + keepPreviousData。
- 刪 `usePaymentData.ts`（死 export handleCreateReceipt/availableOrders 一併清）。
- ✅ **請款頁(requests)接線完成（2026-05-24、type-check+lint+build 全綠、/finance/requests 編入）**：同模式 `useRequestsListView`(伺服器分頁) + serverPagination + 「只看未付」+ server 搜尋(請款單號/團名/訂單號、原頁 searchable=false、順手補)。**校準：tab 範圍改用 `request_category`(tour/company/salary、全 app 事實 SSOT) 而非舊 type guard(request_type 推導、遺留特例)**、現有資料 100% 吻合、更正確且 server query 乾淨。順手修 select 加 branch_id(原 entity select 漏、分公司欄一直顯示「—」)、省掉重的 items join。refreshAll = invalidatePaymentRequests + listView.refresh。

**📊 finance 列表分頁進度盤點（2026-05-24 收尾）**：
- ✅ 收款(receipts list)、✅ 請款(requests list) = 真正的瀏覽列表、已 server 分頁完成。orders/tours 之前已做。
- ⚠️ **出納(disbursement) 跟 金庫總覽(treasury overview) 不是分頁候選**：
  - treasury `/finance/treasury` = **儀表板**（本月收支彙總卡 + 近期交易 top50）、需整月資料算總和、分頁不適用 → 該做的是 **server 端 SUM 聚合**（createReportHook 風格、跨表）、不是分頁。
  - disbursement `/finance/treasury/disbursement` = **批次建撥款單工作流**（要看所有 eligible 請款來勾選 + 銀行分組 + 列印）、分頁會破壞「全選 eligible」→ 該做的是 **server 端只撈 status=pending 的 eligible** 縮範圍、不是分頁。
  - 這兩個是**不同性質的優化（聚合查詢 / 範圍縮小）**、且動財務工作流風險高、不該趕著塞分頁。建議當獨立任務、想清楚再動。
- → **「全撈再前端分頁」的純列表頁已全部轉完**。剩 treasury/disbursement 是聚合/工作流優化、另案。

**⚠️ 並發事故記錄（2026-05-24 20:51）**：我 `git rm usePaymentData.ts` 暫存後、William Mac mini 的並發程序 commit 了 `e8bcb6b feat(workspaces): add beauty industry`、**把我的 usePaymentData.ts 刪除掃進它的美業 commit**（provenance 混亂、且 e8bcb6b 單獨 checkout 會編不過因 page.tsx 還沒提交）。我隨後的收款接線 commit 已恢復 branch tip 一致可編譯。教訓：**這條 branch 有並發 committer、避免長時間留 staged 改動**。

**📌 William 上線後要做 2 件事（在系統「員工→職務權限」勾給角色）**：① 可代墊款 ② 可建立模板（預設關、勾了才生效）。

**待辦 / 待 William**：
- 效能 flagship（Tg msg 535）：RLS auth_initplan 修、動資安層、不緊急、待點頭。見 §3.6。
- 兩個 DB 待 drop（用完參考再通知）：`employee_eligibilities` 表 + `orders.assistant/assistant_id` 欄位。
- 品牌/開團畫面：William「當OK」跳過手動驗、上線後在 prod 看即可。

## 1. branch 現況
- branch：`feat/role-capability-redesign`（從 main、**未 push**、工作樹乾淨全 commit）
- **52 commit**、type-check + 10 守門 + audit:rls + **完整 production build** 全綠、**lint 0 error**（23 既有 error 已全清、見 §3.5）
- 內容：員工職能重構(純角色零旗標) + eligibility/assistant 死碼清乾淨 + 品牌(DB+選品牌+切一半) + 卡住修正(realtime補表) + 產業分類 + **lint 收尾(0a84d02) + 防呆 FK 補齊(b6cb87f)**
- ⏳ **唯一待辦：William 開 app 驗品牌/開團畫面 → OK 即清測試品牌 + push 上線**（5/24 session 收尾）

## 2. ✅ 已完成（這輪）
- **員工職能重構**：純角色 SSOT、零旗標。業務←orders.create/edit.write、團控←tours.members.write、代墊←finance.advance_payment.write、模板←tours.template.write、助理刪除。eligibility 系統整套移除（codegen 重生）。
- **代墊 UX**：Combobox defaultOpen、一步到位。
- **旅遊團列表**：死 code + 白撈訂單清除。
- **role_capabilities 失效修正**：職務權限存檔後失效快取。
- **industry migration**：workspaces.industry/sub_industry + 9 租戶分類（apply prod）。
- **realtime 止血**：4 表（bank_accounts/contracts/expense_categories/payment_methods）進 publication（apply prod）。
- **文件**：體制級地基藍圖（🅐 黏合升級、Realtime 主軸、不上重型引擎）、系統規格書 v1。
- **lint 收尾（0a84d02）**：23 個 main 既有 error 全清（詳 §3.5）。
- **防呆 FK 補齊（b6cb87f、已 apply prod）**：58 張有 created_by 的表全部綁 employees(id) ON DELETE SET NULL（補缺的 27 張、含 receipts/payment_requests/journal_vouchers）。apply 前查證資料乾淨、3 筆孤兒(todos×2/quotes×1)設 NULL、quotes.created_by text→uuid。**紅線 B 系統性對齊完成**（原僅針對新表、此輪回補老表）。
- **合約系統 P1-P5 複盤（agent + Claude spot-check）**：無資安洞/正確性 bug、RLS workspace 隔離 + requireCapability 雙層守門查 DB 證實。

## 3. ⏳ 進行中 / 下一步（落地規格、優先序）
- [~] **① 效能：finance 不全撈** —
  - ✏️ **2026-05-24 B 階段啟動（William「選最高品質」拍板走丙案）**：
    - 決策【丙】：複雜財務頁的體驗「重新設計成分頁原生」—— 「未付」從『排序』改『篩選』（分頁情境更直覺、且不必動 DB sort-priority）。
    - **地基完成**（皆 build/type-check 綠）：`usePaginated` multiSort 多欄排序(662e895) + `ListPageLayout` 加伺服器分頁模式(6d7cf9b、additive 不影響現有頁) + `useReceiptsListView` 收款分頁資料層(7b01c5d)。
    - ⚠️ **EnhancedTable 早已支援 serverPagination**、缺口只在 ListPageLayout 沒接通(已補)。
    - **下一步（收款頁接線、細活勿趕）**：把 `payments/page.tsx` 從 `usePaymentData` 全撈 + 方案A 客端穩定排序、改成吃 `useReceiptsListView`(分頁) + ListPageLayout serverPagination + 「只看未付」篩選。**難點＝樂觀更新(optimisticStatus)+寫入後刷新要從 entity 快取重接到 list-view 快取**(`invalidateReceipts` 不會刷分頁 key、要改呼叫 listView.refresh)。做完→出納→請款(最難)。
    - ✏️ **接線設計定案（2026-05-24 Claude 動手前盤點）**：
      - 查證：`usePaymentData` **只** page.tsx 用；其 `handleCreateReceipt`/`availableOrders`/`orders` 是**死 export**（AddReceiptDialog 自帶 create、BatchDialog 用自己的 useBatchReceiptForm）→ 連帶清掉。
      - 查證：**很多頁透過 entity hook `useReceipts` 讀收款**（tours 財務分頁/finance dashboard/treasury/reports）→ 寫入後**兩個都要刷**：`invalidateReceipts()`(entity、全站一致) + `listView.refresh()`(本頁分頁 key)。難點筆記「改呼叫 refresh」其實要 BOTH。
      - 打法：① 新建 `_hooks/useReceiptMutations.ts`(從 usePaymentData 抽出 confirm/update/delete + optimistic overlay、refresh 改吃 page 傳入的 `refreshAll`)；② page 改吃 `useReceiptsListView`+加 page/search/statusFilter/sort state；③ `refreshAll = invalidateReceipts + listView.refresh` 集中雙刷在 page 一處；④「只看未付」用 headerActions 的 `<Select>`(比照 vouchers 頁、不放 Button)；⑤ 刪 usePaymentData。
      - 型別：`@/stores` 的 `Receipt` = `@/types/receipt.types` re-export(同型別、無不合)。
      - 排序：server sort 預設 `receipt_date desc`、onSort 接列頭點擊。EnhancedTable server 模式內部 sort 已 bypass(processedData=typedData)。
    - ✅ **收款頁接線完成（2026-05-24）**：type-check + lint + production build 全綠。新 hook 名用 `useReceiptListActions`（原想叫 useReceiptMutations 但撞了既有建單 hook）。tab 範圍 server `tour_id/order_id` NULL 判斷 = 舊 type guard `!!` 語意一致(UUID FK 無空字串歧義)。**剩餘 finance 列表：出納(treasury) → 請款(最難)**。
  - 分兩類（原分析）：
  - ✅ **下拉（filter 縮範圍）**：請款單 + 收款單 done（選團才撈該團訂單）。
  - [ ] **列表頁（server 分頁）**：請款管理(usePayments: payment_requests + disbursement 都 all:true)、收款列表、出納… 現在「全撈再前端分頁」。要改 `usePaginated`（createEntityHook 已有、line 443）+ **搜尋/排序/篩選搬 server** + disbursement 狀態交叉處理 + 小計用 count/sum 另查。
    - ⚠️ 請款管理是**複雜頁**：「全部」tab = capability 聯集篩選（tour||company||salary）+ 複合排序（日期 asc + 未付優先）+ disbursement 交叉。**超出 usePaginated 現有 API**（單一 .eq filter + 單欄 orderBy）。→ 要先**擴充 usePaginated（支援 IN filter + 複合 orderBy）或寫自訂分頁查詢**、才能轉。是 careful 重構、做對才不破頁。代表性、改好其他列表照套。**先做這個基礎擴充再轉頁。**
    - 通則：下拉用 filter、列表用分頁（預設 20/頁 15、搜尋走 server、總計另算）。像 Gmail 捲到哪載到哪。
- [~] **② 散刻寫入 → entity hook/apiMutate** — 目標 **127 處 / 53 檔**（清單 `.eslint-suppressions.json`）。
      **Pattern（已確立）**：`supabase.from('X').update().eq('id',id)` → `updateX(id,patch)`（@/data）；`.insert`→`createX`；`.delete().eq('id',id)`→`deleteX(id)`；移除 unused supabase import。複雜的（需 API route）標「需 Claude」。
      進度：✅1 Claude(OrderMembersExpandable.toolbar) + prune stale → 現 **119 處/49 檔**。OpenCLAW 驗證能做(好判斷、不亂猜)。
      **② 真實地圖（按目標表分類、決定打法）：**
      - 🅰 簡單 swap（表**有** entity hook）：order_members(13)、payment_requests(5)、payment_request_items(4)、orders(2)、receipts(2)、notes(2)、calendar_events(2)、employees(2)、tours/itineraries/todos/expense_categories/payment_methods(各1)… → OpenCLAW 照 pattern swap、Claude QC。
      - 🅱 表**無** entity hook、要先建：salary_settlements、selector_field_roles、workspace_roles、tour_role_assignments、tour_* config 表、user_preferences… → Claude 建 hook 再遷。
      - 🅲 **可能 intentional 豁免**：journal_vouchers(13)+journal_lines(11)=會計複式分錄（CLAUDE.md 紅線 E 提過 journal_lines intentional dual-write）、LINE 整合(line_user_profiles/messages)、ref_* master 表。→ 先確認是否該豁免、加 allowlist comment、不硬遷。
      打法：先清 🅰（量大又安全）→ 🅱 建 hook → 🅲 確認豁免。
      ✏️ **校準（含自我更正）**：規則(`eslint-rules/venturo-design-system.js:741`)**已排除 API route + /lib + /data** → 所以這 116 是**真的 client 端寫入**（pages/components/services in (main)+/components/）。我一度誤判「規則 over-flag API」、錯了（order_members 那 13 處在 api/、本來就沒被算進來）。
      - 116 仍**異質**、逐檔判斷：① 單列 CRUD→entity hook swap（已做 toolbar/2 receipts/archive）；② cascade/bulk delete by tour_id→無法用單列 hook、需 invalidate-after 或 API route；③ 審計 log(order_status_logs)→可能豁免；④ 無 hook 的表(checks…)→先建 hook。
      - **能簡單 swap 的客戶端寫入大致清完**（剩多在 services/cascade/log/無hook）→ 剩下是真功夫：建 hook + cascade 失效 + 確認豁免。不能 bulk、Claude 逐檔。
      - 進度：**114 處**（127→114）。已遷移：toolbar(tours)、receipts×2、archive(還原 tour)、CreateCheckDialog(建 checks hook)、OrderStatusChangeDialog(orders)。+ usePaginated 加 IN-filter。
      - ✅✅ **高價值 client-component 寫入（會造成「刪改畫面不動」的）已大致做完**。剩 114 多是：cascade 清理（刪 tour 時連帶刪 itinerary/calendar、刪掉的資料無需失效）、審計 log（order_status_logs 等、fire-and-forget 合法）、services（多在刪除流程）、需 API route 化的（大工）。→ **SSOT 完整的「會痛的部分」已解；剩餘是低價值清理或大重構**。
      - ✏️ **誠實 ceiling**：clean 單列 client swap **基本做完**（5 檔）。剩 115 多是：services 的 cascade/bulk（unlink/delete by tour_id、不合單列 hook）、多步流程、審計 log、需 API route 化的 —— **是「真重構」不是「換寫法」**（每個要判斷：轉 API route+apiMutate / 補 invalidate-after / 豁免 / 建 hook）。已示範全部 pattern。剩下是 deliberate 重構程序、宜系統性逐塊做。
- [~] **③ 清理不相干 code/資源**（強迫症乾淨）
  - ✅ **isEligibility 旗標系統徹底移除**（role 重構尾巴）：型別欄位 + deriveEligibilityList 死函數 + codegen + 6 consumer + sync script + 文件全清。零行為改變。
  - ✅ **訂單 assistant 死欄位移除**（助理職務尾巴）：entity select 不撈 + 型別不宣告。
  - 兩張 DB 資料保留作 deploy 參考、待 William 用完 drop：`employee_eligibilities`(52列) + `orders.assistant/assistant_id`(40+列)。
  - [ ] 可續：更廣 dead-code / unused-export 掃描（diminishing returns、視需要）。
- [ ] **④ CLAUDE.md 等邏輯級 SOP 對齊規格書**
- [~] **⑤ 品牌 brand 功能**（William 拍板做、勁揚已刪不用管資料、架構做好從新加）：
  - ✅ **DB + 資料層地基 done**：tours 加 brand_id（FK→brands、61 團回填預設品牌、已 apply）+ brands entity hook（useBrands/createBrand…）。migration `20260524074500`。
  - [ ] **brand_id type**：types.ts tours Row 待加 brand_id（用到時 regen 或手加 3 處 near controller_id line 9183/9258/9333）。
  - ✏️ **更正（我前面誤判「無管理 UI」）**：品牌管理**已存在**！租戶創建 `TenantBrandSection`（初步管理設品牌）+ 公司設定 `settings/company OrganizationSection/DimensionSection`（管 brands/branches、API `/api/organization/brands`）。**原設計本就 show-if-multi**（OrganizationSection 註解：「某維度新增第 2 筆、業務單據自動冒對應下拉」）。
  - ✅ **開團選品牌 show-if-multi 完成**：tours.brand_id 接型別 + 建團 payload 持久化 + TourBasicInfo 自動帶預設品牌(單一不問)+ >1 才顯示品牌下拉。**需跑 app 驗顯示**。
  - ✅ **品牌模型定案（William msg 524「通知我們」）**：品牌由漫途設（租戶創建 + 公司通知漫途加）、租戶不日常自管。
  - ✅ **公司設定移除品牌管理**（William msg 529、漫途追加）+ 刪 dead DimensionSection。
  - ✅ **開團「品牌|團控」並排切一半**（多品牌 grid-cols-2、單一品牌只剩團控全寬）+ 品牌必選（auto-default + *）。
  - → **品牌功能完成**（DB + 開團選品牌 show-if-multi + 並排 + 公司設定移除 + scope 取消）。**全部需跑 app 驗顯示**。
  - [ ] **ConvertToTourDialog**（提案→正式團）若沒走 TourBasicInfo、品牌可能要補（auto-default effect 在 TourBasicInfo、若 convert 用它則自動覆蓋）。
  - ❌ **品牌 scope 取消**（William msg 527：品牌不分、公司內所有人都看得到所有品牌案子）→ 省掉最大/最有風險的 RLS 塊。
  - [ ] 多品牌列表顯示/篩選品牌欄（可選、非核心、show-if-multi）。
  - → **品牌功能幾乎完成**（貼品牌+選品牌+管理 done、scope 不用）、剩公司設定 hide-if-single 小收尾。
  - [ ] **品牌 scope**：員工按 employee_brands 只看自己品牌的案子（動 RLS、測登入 4/20）。
  - 評估 + 6 層計劃：`2026-05-24-品牌brand實作評估.md`。

## 3.5 QC 發現（main 既有、非本支線引入）
- ✅✏️ **2026-05-24 已全清（commit 0a84d02）**：William 拍板「小毛病也要修」。23 個全修根本原因：
  - 13× img → 移除引用「未載入規則」的 dead disable 註解（flat config 刻意不載 next plugin）
  - 2× useSWR → 抽成 `src/data/hooks/`（useMessagingConversations / usePaymentProviders、比照 useRoles）
  - 2× no-unused-expressions → 三元改 if/else
  - 6× no-in-loop-number-rpc → **逐一查證皆正確寫法**（5× insert-between：await createX commit 後才生下一號；1× create-store 撞號重試迴圈）→ 加查證過 scoped disable + WHY。**校正：批次化只 3/6 適用(同團)、2 個跨團、收益微小、不值得動財務編號 RPC**。
  - 驗證：lint 0 / type-check 0 / build 綠。
- ⬇️ 以下為當時原始評估（保留 audit trail）：
- **lint 23 errors / 13 檔**（`npx eslint --suppressions-location .eslint-suppressions.json --quiet .`）：
  - 多數 `@next/next/no-img-element` "Definition for rule not found" = eslint flat config 沒載 next plugin、inline disable 引用未知 rule（設定問題）。
  - `venturo/no-in-loop-number-rpc`（財務 useBatchReceiptForm / useReceiptMutations / AddRequestDialog.submit）：迴圈內逐筆 `generateReceiptNo/RequestNo`。**精準評估：number RPC 有 advisory lock（@/lib/codes）→ 不會真撞號、是 N 次 round-trip 的效能問題**、非 live 正確性洞。fix = 改批次 wrapper `generateReceiptNos`。
  - `no-unused-expressions`（error.tsx / global-error.tsx）。
  - 2 個 `no-direct-useswr-in-pages`（finance/requests 頁、baseline）。
  - **本支線 71 檔改動 ∩ error 檔 = ∅**（零新 error）。
  - 建議：另開小支線修這 23（混進本支線不乾淨）；注意若本支線開 PR、CI 跑 lint 可能被既有 error 擋。

## 3.6 效能盤點（5/24「繼續優化」）— 見 `architecture/2026-05-24-效能-Supabase-advisor-發現.md`
- **列表分頁好做的已做完**：orders（useOrdersListView）+ tours（useToursPage）已 server 分頁。
- 剩財務列表（請款/收款/出納）= HARD（模糊分類 + capability tab + 自訂排序 + 需 schema）。
- **Supabase advisor**：auth_rls_initplan(46,最高價值,動資安層) / unindexed_FK(127,judgment) / unused_index(122,DB新不可靠勿drop) / multiple_permissive(40) / duplicate_index(4,安全小win)。
- **flagship 建議**：auth_rls_initplan 修（RLS 逐列→每查一次、semantics-preserving、登入+跨租戶測試證明安全）。動資安層、待 William 點頭。
- ⚠️ DB 流量低（122 unused index 為證）→ RLS initplan 是 at-scale 優化、不緊急。
- **發 Tg 問 William 方向（msg 535）**。

## 4. 關鍵決策紀錄
- 🅐 黏合升級（Supabase Realtime 主軸）、**不**導入重型同步引擎（PowerSync/Electric）— 辦公室 ERP 不需離線/協作/差量；觸發點=離線優先變核心才重評。
- realtime 層其實已穩（auth race / channel 撞號 / 訂閱觀測 5/20 已修）；卡住真因=publication 漏(已修) + 110 散刻寫入(待清)。
- auth/realtime token 同步正常（auth-sync onAuthStateChange）、不重建。
- 代墊/模板能力預設關（🅐 by William）、deploy 後在職務權限勾給角色。

## 5. 待 William / deploy 注意
- deploy（push branch）後 2 步：職務權限勾「可代墊款」「可建立模板」給該角色。
- 那幾筆未被出納勾的請款、deploy 後重存即可（William 同意）。
- **兩個待 drop（William 用完參考後通知）**：`employee_eligibilities` 表(舊逐人指派、可參考誰該給代墊/模板) + `orders.assistant/assistant_id` 欄位(舊助理紀錄)。code 已不讀、純 DB 殘留。
- 細節文件：`2026-05-24-體制級SSOT-資料地基藍圖.md`、`2026-05-24-員工職能-capability簡化方向.md`、`2026-05-24-SWR快取失效分析.md`
