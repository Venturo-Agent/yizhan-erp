# yizhan-erp 架構整理 backlog

> 盤點日期：2026-05-29。來源：10 個子系統架構盤點 + smells。
> 目的：一批一批慢慢重整。每批都能**單獨 commit**、改的檔案清單明確、不可 `git add .`。
> 嚴重度標記沿用盤點原始 severity，規模 S/M/L 為粗估。
>
> **復盤紀錄 2026-05-29**：抽查 B2/B6/B7/B9/B11/B13 共 8 條 file:line，逐條核對原始碼全部屬實。唯一誤差：B13 撞時間戳盤點寫 31 對、實際 29 對（已修正）。其餘證據可信。

---

## 建議執行順序（風險低 → 高、相依在前）

| 順序 | 批次 | 規模 | 相依 | 為何排這 |
|---|---|---|---|---|
| 1 | ~~B1 純垃圾清掃~~ ✅ no-op 2026-05-29（.trae/tsbuildinfo/lint-report 皆未 tracked、gitignore 已涵蓋） | S | 無 | 零邏輯風險、純刪檔/補 gitignore，先把噪音清掉 |
| 2 | ~~B2 dead code 收斂~~ ✅ 已做 2026-05-29（刪 4 處死碼，commit 1079327；sentry-check/CACHE_PRESETS 延後） | S | 無 | 都是無人消費的死碼，刪了不影響行為 |
| 3 | ~~B3 prettier drift~~ ✅ no-op 2026-05-29（src 0 drift、已被清過；CLAUDE.md「771 檔」記載過時） | M | 無 | 純格式、單獨一個大 commit，後續批次 diff 才乾淨 |
| 4 | ~~B4 雙 lockfile / 套件管理器單一化~~ ✅ 已做 2026-05-29（刪 bun.lock，commit 13fe484） | S | 無 | 決策性小，但會影響 CI；先定再說 |
| 5 | ~~B5 憲法文件 SSOT 收斂（CLAUDE.md vs draft）~~ ✅ 已做 2026-05-29（採拆檔版，commit fec37a4） | S | 無 | 文件層、無 runtime 風險，但要先定才能指導後面批次 |
| 6 | B6 deprecated module + 路由↔module↔sidebar 對齊 | M | B2 | 先清死碼再對齊三套路由真相 |
| 7 | B7 status 顯示 3 套 SSOT 收斂 | M | 無 | 影響面廣但機械式，可先做 |
| 8 | B8 constants / types / core 分層收斂 | M | 無 | 命名與檔目錄整併，import 改動多但可逐檔 |
| 9 | B9 API 錯誤回應 + capability 守門 + audit 收斂 | L | 無 | 後端行為相關、需逐 route 驗，風險中高 |
| 10 | B10 workspace_id 取得管道收斂（client 多源 + 紅線 H） | M | B9 | 牽動授權，需與 B9 一起看 session 取值 |
| 11 | B11 讀取層多引擎收斂（紅線 F 主戰場） | L | B6, B10 | 全站讀寫核心、風險最高，放最後且分多個子 commit |
| 12 | B12 UI 散刻收斂（表格/日期/dialog/input 多份 + raw HTML） | L | 無（可與 B11 並行） | 影響面巨大、需逐頁，分多 commit 慢慢來 |
| 13 | B13 migration 目錄衛生 | M | 無 | 歷史不可改，多為流程/文件整頓，獨立進行 |

> 並行建議：B7/B8/B12/B13 彼此無相依，可在不同分支同時推進。B11 是最後的硬骨頭，務必等 B6/B10 先把上游真相對齊。

---

## B1 — 根目錄雜物 / IDE 殘留 / build artifact 清掃

**問題**：根目錄混了一堆不該進 repo 的衍生產物與 IDE 殘留，加上整個工作樹存在第二份完整副本，容易誤改錯樹。

**證據**：
- `tsconfig.tsbuildinfo`（682KB build artifact，在根目錄）— 需確認 `.gitignore` 涵蓋
- 根目錄 `lint-report.json`（lint:report 輸出，已 eslint ignore 但仍是衍生物）
- `.trae/documents/VENTURO-APP-建設計劃.md`、`.trae/documents/VENTURO-APP-登入頁建設計劃.md`（TRAE IDE 殘留）
- 第二份工作樹 `/Users/williamchien/Projects/TRAE/yizhan-erp`（同一 git remote，`src/lib/codes.ts` 已 diff 不同，還留 `lib/eligibilities.ts`、`lib/demo/`、`lib/yongcheng/`、`lib/pdf/` 等本樹已無舊檔）

**影響面/風險**：低。純清檔。第二份工作樹屬本機環境問題，非 repo 內容。

**建議動作**：
1. 把 `tsconfig.tsbuildinfo`、`lint-report.json` 加進 `.gitignore`，並 `git rm --cached`。
2. `.trae/` 加進 `.gitignore` 並 `git rm -r --cached .trae`。
3. 第二份工作樹：本機決定刪除或標記為唯讀（不進 commit，僅紀錄於 backlog）。

**規模**：S　**相依**：無

---

## B2 — dead code / 孤兒 export / demo route 收斂

**問題**：多處宣告了卻無人消費的死碼、相容性殘留、診斷用 route 留在 production。

**證據**：
- `src/components/ui/input.tsx:152-153` — `const _Input = Input`（未 export、未引用）
- `src/components/ui/use-toast.ts:10` — `useToast` 0 外部消費者（實際走 sonner）
- `src/stores/index.ts:64-67` — `const _useEmployeeStore = createStore<Employee>(...)`（宣告後從未匯出）
- `src/lib/status-config.ts` — 純 re-export，只剩 `src/components/table-cells/status-cells.tsx` 1 個 importer（→併入 B7）
- `src/lib/rbac-config.ts` — 全檔只剩 `type UserRole`，檔名誤導（→可順手改名，見 B8）
- `src/data/core/types.ts:57-82` — `CACHE_PRESETS` high/medium/low 內容幾乎相同（假分類）
- `src/data/core/createEntityHook.ts:163` + `types.ts:139` — `all` 參數已死僅留相容
- `src/app/api/_test/sentry-check/route.ts` — 診斷 route 部署後即可達端點
- `src/data/entities/members.ts` — `useMember/Paginated/Dictionary/createMember/invalidate` 以 `_` 命名留著不 export（dead，與 B11 的 order_members 雙引擎同源）

**影響面/風險**：低。刪除無消費者的 export 不改變行為。`_test` route 需確認無人靠它做健康檢查。

**建議動作**：逐項刪除死碼；`_test/sentry-check` 改為 production 環境關閉或移到非部署路徑；CACHE_PRESETS 簡化為單一 preset（或留 TODO 與 B11 一起處理）。`status-config.ts` 留給 B7。

**規模**：S　**相依**：無

---

## B3 — prettier drift 一次性 reformat

**問題**：771 檔未過 prettier、Code Quality Check 長紅、pre-commit 只擋 staged 沒擋存量（來自既有紀錄 + tooling 盤點）。

**證據**：tooling 子系統 — `.husky/pre-commit` 只跑 staged-only `prettier --check`；CI `standards-check.yml` / `ci.yml` 對全量檢查長紅。

**影響面/風險**：低（純格式）但 diff 巨大。

**建議動作**：
1. 單獨一個 commit 跑 `prettier --write` 全量。
2. 不混任何邏輯改動（這個 commit 只有格式）。
3. 完成後其餘批次的 diff 才會乾淨。

**規模**：M（檔多但機械）　**相依**：無（建議排在所有大型 import 改動之前，避免重複 reformat）

---

## B4 — 雙 lockfile / 套件管理器單一化　✅ 已完成 2026-05-29

> 已刪 `bun.lock`（commit 13fe484）。確認 nixpacks.toml + 6 個 CI workflow 全走 `npm ci`，保留 `package-lock.json` 為唯一鎖檔。

**問題**：`bun.lock` 與 `package-lock.json` 並存，套件管理器無單一真相，CI 與本地可能裝出不同 `node_modules`。

**證據**：根目錄 `bun.lock`（428KB, 5/28）+ `package-lock.json`（452KB, 5/28）並存；`package.json` scripts 全用 `npm run` / `npx husky`。

**影響面/風險**：中。刪錯 lockfile 會影響 CI 安裝。

**建議動作**：確認 CI 與部署（Coolify / nixpacks.toml）實際用哪個管理器 → 保留一個、刪另一個、在 README/CLAUDE.md 明定。鑑於 scripts 全 npm，傾向保留 `package-lock.json` 並移除 `bun.lock`（需先確認沒人用 bun 裝）。

**規模**：S　**相依**：無

---

## B5 — 憲法文件 SSOT 收斂（CLAUDE.md vs draft）　✅ 已完成 2026-05-29

> 已採拆檔版（commit fec37a4）：`CLAUDE.md` 精簡為 219 行主檔 + `docs/rules/` 五份細節。原 52KB 全文版已被取代、draft 已轉正刪除。

**問題**：兩份憲法並存且內容大幅分歧，讀者/協作 bot 不知哪份為準。

**證據**：`CLAUDE.md`（52KB，標 2026-05-13 重寫）與 `CLAUDE.md.draft.md`（11KB，5/29 標「拆檔版」指向 `docs/rules/`）。

**影響面/風險**：低（文件層）但會誤導後續所有重整方向。

**建議動作**：決定最終形態（拆檔版 vs 全文版）→ 完成遷移後刪除另一份；若拆檔，確認 `docs/rules/` 已就位。建議排在 B6–B13 之前定案，作為整理依據。

**規模**：S　**相依**：無

---

## B6 — deprecated module + 路由↔module↔sidebar 三套真相對齊

**問題**：module 描述子（34）、`(main)` 頂層路由（22）、sidebar 順序三者手動對齊、無 CI 校驗，且含已死的 bot/messaging module 與半成品 office。

**證據**：
- 已 deprecated 仍留檔：`src/modules/{line_bot,facebook_bot,instagram_bot,messaging_inbox}.ts`（`_registry.ts:18-20` 註明已併 AiHub、不 register）
- `(main)/bot/`、`(main)/messaging/` 目錄仍實體存在（bot 含 setup/facebook-setup/instagram-setup 子頁）— 需確認是否仍可達、該與 AI Hub 整併
- office：`src/modules/office.ts:19,22` routes:[] tabs:[] 仍 active、會產空 capability 與 HR 空 entry、`(main)` 無對應目錄
- `office.ts:11-12` 自註 features.ts / module-tabs.ts 間語意不一致
- sidebar 漏列：`sidebar-config.ts` SIDEBAR_ORDER(line 98-118) 未含 esim/visas/bot/messaging/settings，但這些 page 都存在 → 孤兒入口
- 第二套手寫路由映射：`src/lib/permissions/index.ts:35-47` FEATURE_PERMISSIONS + `:53-74` ROUTE_TO_MODULE，與 `features.ts`(@generated) 的 routes[] 漂移；`index.ts:71 '/bot':'line_bot'`（指向已廢 module）、`:64 '/office':'office'`（指向空功能）
- `capabilities.ts:184-189` Legacy 區（channels.manage / line_bot.* / settings.env.write）無 modules SOURCE 對應
- sidebar href 全硬編、與 module.routes 與實體目錄三套需人工同步、無 CI 校驗

**影響面/風險**：中。動 module registry 會經 codegen 連動 capabilities/features/module-tabs，需重跑 codegen 並驗權限。

**建議動作**：
1. 拍板 bot/messaging/office 去留；確定刪除者連同 `(main)/bot`、`(main)/messaging` 目錄、deprecated `*_bot.ts` / `messaging_inbox.ts` 一起移除，重跑 `npm run codegen:permissions`。
2. 收斂路由真相為單一來源：刪 `permissions/index.ts` 的手寫 ROUTE_TO_MODULE/FEATURE_PERMISSIONS，統一用 `features.ts` 的 routes[]（或反之，但只留一套）。
3. 補一支 audit：校驗 sidebar href ↔ `(main)` 目錄 ↔ module.routes 三者一致。
4. 清 `capabilities.ts` Legacy 區（已廢 line_bot.* capability）。

**規模**：M　**相依**：B2（先清死碼）

---

## B7 — status 顯示 3 套 SSOT 收斂

**問題**：狀態 label/color/tone 有三套並存、職責重疊，且各自宣稱是 SSOT。

**證據**：
- `src/lib/status/index.ts`（getStatusLabel/Color，35 importer）
- `src/lib/constants/status-maps.ts`（getPaymentStatusLabel/getTourStatusLabel，16 importer，252 行）
- `src/lib/design/status-tone-map.ts`（STATUS_TONE_MAP，5 importer，註解自稱「全站 Status SSOT」與 status/ 衝突）
- `src/lib/status-config.ts`（純 re-export，1 importer `table-cells/status-cells.tsx`）

**影響面/風險**：中。改 import 點多（~57 處）但機械式，行為應等價。

**建議動作**：選 `src/lib/status/` 為唯一 SSOT → 把 status-maps、status-tone-map 的 label 來源收進去（tone map 可保留為純樣式映射但 label 取自 status/）→ 改 16+5+1 個 importer → 刪 `status-config.ts`。逐 importer 改，可分數個小 commit。

**規模**：M　**相依**：無

---

## B8 — constants / types / core 分層與命名收斂

**問題**：常數有兩套根、labels 檔目錄 shadow、tour 型別雙位置、core 過瘦職責不清、命名公約混亂。

**證據**：
- 兩套 constants 根：`src/constants/`(labels/payment-provider/payment-method/messages) 與 `src/lib/constants/`(tour-status/airports/status-maps/tour-service-types/timeouts/layout)
- labels 檔/目錄 shadow：`src/constants/labels.ts` 與 `src/constants/labels/`(index/workspace/quotes/accounting) 並存，`@/constants/labels` 解析到 .ts、子檔成孤兒
- tour 型別三處分裂：`src/types/tour.types.ts`(45) + `src/types/tour/`(tour-core/itinerary/member/flight/index) + `src/types/flight.types.ts`(61)
- `src/core/` 僅 3 檔（types/common.ts、errors/app-errors.ts、services/base.service.ts），common 型別放 core 而非 src/types，分層模糊
- 命名不一致：扁平 `*.types.ts` vs `database/index.ts` 無後綴 vs `tour/` 內又回 `*.types.ts`
- `src/lib/rbac-config.ts` 名實不符（只剩一個 type，'admin' 字面與紅線 0 張力）

**影響面/風險**：中。純型別/常數整併，import 改動多但無 runtime 邏輯。

**建議動作**：
1. 定 constants 單一根（建議全收進 `src/constants/`，把 `src/lib/constants/*` 搬過去，status-maps 交給 B7）。
2. 解決 labels shadow：合併 `labels.ts` 進 `labels/index.ts`，刪 `labels.ts`。
3. tour 型別合一：統一進 `src/types/tour/`，刪扁平 `tour.types.ts`，flight 收進 tour/ 或保留頂層但二擇一。
4. core 三檔：決定併入 `src/types`/`src/lib` 或補實職責。
5. `rbac-config.ts` 改名或併入 `src/types/user.types.ts`。
逐項一個 commit。

**規模**：M　**相依**：無（status-maps 部分與 B7 協調）

---

## B9 — API 層 SSOT 收斂（錯誤回應 / capability 守門 / audit）

**問題**：API route 的三個橫切關注點各有多套寫法或覆蓋不全，難以一眼 grep 判定哪 route 有沒守。

**證據**：
- 錯誤回應分裂：`db-error-translate.ts` 同檔 export `dbErrorResponse`(回 Response) 與 `translateDbError`(回訊息)；161 route 僅 22 檔用 dbErrorResponse；~70 個帶 insert/update/upsert/delete 的 route 未走 dbErrorResponse（含 accounting/{opening-balances,period-closing,vouchers/create,vouchers/[id]/reverse,receipts/[id]/refund}、hr/salary-settlements/*、roles/[roleId]、workspaces/[id]/*、tenants/create）
- capability 守門三套：`requireCapability`(106 route)、`getApiContext({capabilityCode})`(approvals PATCH、disbursement/batch-create:L67)、直接 import `hasCapabilityByCode`(workspaces/[id]:L36-37 條件式 self-vs-cross-workspace)
- audit 覆蓋不全：`recordApiAuditContext` 161 route 僅 77 檔引用，~84 route 無 audit actor/reason
- 純函式 helper 混置 route 目錄：ocr/passport、disbursement/[id]、tenants/create、accounting/vouchers/auto-create（純邏輯未下沉 src/lib）
- hardcode workspace UUID fallback：`workspaces/[id]/route.ts:L13-14 CORNER_WORKSPACE_ID = ... || '8ef05a74-...'`

**影響面/風險**：中高。動授權與錯誤回應屬後端行為，需逐 route 驗證並跑 audit。

**建議動作**：
1. 錯誤回應：訂 `dbErrorResponse` 為 API catch 唯一出口，逐批把 ~70 route 的 catch 改過去（一個資源資料夾一個 commit）。
2. capability：收斂為單一守門 helper（建議 `requireCapability` + 在其上支援條件式 self-vs-cross-workspace），逐步退役 getApiContext/直接 import 寫法。
3. audit：對寫操作 route 漸進補 `recordApiAuditContext`。
4. 純函式 helper 下沉 `src/lib`。
5. 移除 hardcode workspace UUID fallback。
建議每個 route 資源資料夾單獨 commit，不要一次掃全部。

**規模**：L　**相依**：無

---

## B10 — workspace_id 取得管道收斂（client 多源 + 紅線 H）

**問題**：client 端取 workspace_id 散在多個 lib 檔、同名 server 版混淆；部分 route 從 client 取 workspace_id（紅線 H 風險）。

**證據**：
- client 三檔重疊：`src/lib/workspace-helpers.ts`(getCurrentWorkspaceId client 版,2 importer)、`workspace-context.ts`(getWorkspaceId/getRequiredWorkspaceId,8 importer)、`supabase/api-client.ts`(getCurrentWorkspaceId **server** 版,22 importer) — 同名 getCurrentWorkspaceId 兩處不同實作
- 讀取層雙來源：`createEntityHook.ts:121-133` useAuth 走 zustand store；但 `applyWorkspaceScope`(:145-149) 與 `entityHookCrud.ts:69` 改呼 `getCurrentUserContext()`(`entityHookCache.ts:50-66` 直讀 localStorage 手解 JSON)
- 紅線 H 風險 route：auth/sync-employee、permissions/features、integrations/usage、public/invoices/[token]/pay、workspace-integrations 從 searchParams/body 取 workspace_id — 需逐檔確認是否為無 workspace 欄位的例外表或真漏洞

**影響面/風險**：中。牽動多租戶 scope，hydration timing 不一致可能讀錯租戶。

**建議動作**：
1. client 端統一一個 `getWorkspaceId` 入口（zustand store 為準），合併三檔；server 版同名函式改名避免混淆。
2. 讀取層統一從 store 取（與 B11 協調）。
3. 逐檔審查紅線 H 風險 route，確認例外表或修正為 session 取值。

**規模**：M　**相依**：B9（與 session 取值一起看）

---

## B11 — 讀取層多引擎收斂（紅線 F 主戰場）

**問題**：紅線 F「讀資料只走 entity hook」未收斂，實際至少四套讀取引擎並存，加上同表雙建、realtime 訂閱無單一 SSOT、stores 的 createStore 也撞職責。

**證據**：
- 四套讀取引擎：(1) `createEntityHook` 工廠(52 entity)；(2) `lib/swr/createReportHook.ts` + `data/entities/finance-summary.ts:13-14,70,125`(直 useSWR+customRpc.rpc)；(3) `src/data/hooks/` 六個手寫 useSWR(`useRoles.ts:32`/`useBranches.ts:25` wrap REST、`useCountryAirports.ts:28-29` 直 supabase.from)；(4) `role-capabilities.ts:13-14,34` plain useSWR + supabase.from
- 同表雙引擎：`src/data/entities/members.ts:18` 與 `order-members.ts:17` 同表 `order_members` 各建一套（cache/realtime 分裂、select 不同、型別不同）
- stores 撞職責：`src/stores/index.ts:28-58` createStore 產 useTourStore/useOrderStore/useQuoteStore（帶 listFields、直接 supabase fetch），與 @/data entity hook 並存兩條路徑
- realtime 無 SSOT，3 套 + ~9 處散刻：`lib/swr/use-realtime-mutate.ts`(自稱 SSOT,4 caller)、`data/core/entityHookRealtime.ts`(主力)、散刻 supabase.channel 於 calendar/_hooks/useCalendarEvents、tours/_hooks/useToursPaginated、orders/_hooks/useOrderMembersRealtime、stores/core/create-store、hooks/useEditingPresence、hooks/use-realtime-reload
- workspace 隔離雙軌：`createEntityHook.ts:80 config.workspaceScoped ?? WORKSPACE_SCOPED_TABLES.includes(...)`，`entityHookCache.ts:108-162` 硬編 ~40 table 名單（自註應萎縮但未收斂）
- `src/data/hooks` 與 `src/data/entities` 兩條路徑同層、payment-methods 兩處(`entities/index.ts:472` vs `hooks` usePaymentMethodsCached)、hooks barrel 漏 export 3 個(useMessagingConversations/usePaymentProviders/useCountryAirports)
- `restaurants.ts:41 createEntityHook<Attraction>('restaurants')` 型別錯用
- 頁面層凍結 17 筆 no-direct-useswr-in-pages baseline（`.eslint-suppressions.json`）

**影響面/風險**：高。這是全站讀寫核心，動它影響每個業務頁。務必分多個子 commit、逐 entity 驗。

**建議動作**（分子任務、各自 commit）：
1. **先做 order_members 雙引擎合一**（B2 已標 members.ts 死碼）：保留一套、改 caller、刪另一套。
2. `restaurants.ts` 修型別。
3. stores 的 useTourStore/useOrderStore/useQuoteStore 逐個遷移到 entity hook，遷完刪 createStore（與 stores 子系統一起）。
4. realtime 收斂到 `entityHookRealtime` 的 useRealtimeSync，逐處替換 6+ 散刻 supabase.channel。
5. data/hooks 六個手寫 hook 評估能否走工廠；不能的明確標註例外並補進 barrel。
6. WORKSPACE_SCOPED_TABLES 名單逐表搬進 entity config，萎縮硬編名單。
7. finance-summary / createReportHook 屬 RPC 彙總，明確定義為「報表引擎」例外、文件化邊界（不強行併入 entity hook）。

**規模**：L　**相依**：B6（路由/module 真相先穩）、B10（workspace_id 來源先統一）

---

## B12 — UI 散刻收斂（多份 primitive + raw HTML 繞過 SSOT）

**問題**：同一關注點多份 primitive 並存、職責邊界模糊；表格/日期/輸入有 SSOT 卻被業務頁 raw HTML 繞過。注意：components 內部 token 紀律其實乾淨（0 預設色），重災在「多份 primitive」與「src/app 業務頁繞過」。

**證據**：
- 表格三處 + 51 檔 raw `<table>` 繞過 EnhancedTable(38 consumers)：`ui/enhanced-table/` + `ui/inline-edit-table/`(~10 consumers) + 頂層 `table-cells/`；raw table 見 accounting/reports/{trial-balance,general-ledger}/page.tsx、finance/treasury/_disbursement/_components/DisbursementRequestsTable.tsx、orders/_quotes/_components/QuoteCostTable.tsx 等
- 日期 5 份：`ui/{date-input,date-picker,simple-date-input,time-input,calendar}.tsx`；DatePicker 32 檔、SimpleDateInput 3 檔、DateInput 幾乎孤兒、raw `<input type=date>` 6 檔
- Dialog 三層：`ui/dialog`(71 importer) + `dialog/form-dialog`(46) + `shared/EntityFormDialog`；另有手刻 `fixed inset-0` modal 6 檔（ai/_components/AiConversationsTab、orders/_quotes/_components/PrintableQuickQuote、websites/design/page 等）
- 輸入 4 份：`ui/{input,input-ime,calc-input,rich-text-input}.tsx`（input.tsx 已內建全半形+數學+IME，:30-117）
- 重複 primitive：badge+status-badge、empty-state+empty-value、spinner+skeleton、form-field+form-label+field-error+label
- 缺 common/ 層：頂層散檔 ErrorBoundary/ErrorLogger/bank-combobox/module-error/module-loading/seal-image/unauthorized-page/AppInitializer/EditingWarningBanner 與 shared/selectors/dialog/widgets 平行、無歸屬規則
- 散刻 supabase.from 寫入 baseline 凍結 127 個（ratchet）、API 直寫 92 檔（容許現狀僅擋新增）

**影響面/風險**：高。涉及大量業務頁逐頁改，且報表/列印型 raw table 未必適用 EnhancedTable。

**建議動作**（分子任務、各自 commit）：
1. 先定 primitive 收斂規則（哪個為準、職責邊界）寫進 CLAUDE.md/設計文件。
2. 日期：定 DatePicker 為主入口，DateInput 死碼清掉，逐步收 simple-date-input。
3. Dialog：明確三層職責（ui/dialog 底層 / FormDialog 表單 / EntityFormDialog 實體），手刻 modal 6 檔改用 FormDialog。
4. 輸入：input-ime/calc-input 評估能否併入 input.tsx。
5. badge/empty/loading/form 片段：定規則保留主件、其餘標記或合併。
6. raw table：報表/列印型標為合理例外、列表型逐頁改 EnhancedTable。
逐頁、逐 primitive 小 commit，不可一次大改。

**規模**：L　**相依**：無（可與 B11 並行，但避免同檔衝突）

---

## B13 — supabase migration 目錄衛生

**問題**：migration 目錄有時間戳衝突、seed SSOT 名實不符、雙草稿區、停用檔留樹、過時反模式文件、漂移修補與一次性運維資料混在序列。歷史不可改，重點在「流程/文件/草稿區」整頓而非改既有 migration。

**證據**：
- 29 對 migration 共用完全相同 14 位時間戳（`uniq -d` 複核 2026-05-29，盤點原寫 31，實際 29），apply 排序退化為檔名次序、相依不可靠（如 20260510120000 onboarding 對 phase_a1、20260514* 多組）
- `config.toml:63 sql_paths=["./seed.sql"]` + seed.enabled=true，但 `seed.sql` 不存在；seed 散在 20 個 `seed_*.sql`
- 雙草稿區：`supabase/migrations-pending/`(SOP 指定) 與 `migrations/_pending_review/`(3 檔) + `migrations/_rejected/`(7 檔含 2026-04-18/)；且 001/002/003 已以 20260509080537~080539 進正式序列、草稿與落地版同時存在
- `.sql.skip` 停用檔被 git tracked：`20260415000000_enable_ai_settings_rls.sql.skip`、`20260415110000_create_audit_logs.sql.skip`（audit_logs 實際由 080538_002 建，一功能兩來源檔）
- `APPLY_MIGRATION.md` 教人在 Studio SQL Editor 手跑 DDL，直接矛盾 Migration SOP
- 漂移證據：`20260510235811_repair_production_schema.sql`(32KB)、`schema_drift_fix_3_tables`、`fix_smoke_findings`、`fix_itineraries_drift`
- 一次性運維資料入序列：fix_william_supabase_user_id ×4、reset_utour_employees_password、create/remove logan_ai、create/remove hosanna_workspace 等
- build-then-drop / create-twice 短命 migration（knowledge_base ×2、add_simple_mode_fields ×2、tour_documents 同日 create/drop）
- RLS 中央 procedure 採用率低：`20260513024000` 後僅 22 用 setup_workspace_scoped_rls，但 198 檔仍 inline CREATE POLICY
- 遊離 `CLEAN_MIGRATION.sql`（不在序列、用舊 auth.uid() pattern）

**影響面/風險**：中。既有 migration 不可改（歷史），但時間戳衝突若涉相依需謹慎處理；多數動作是文件/草稿區整頓。

**建議動作**：
1. 流程文件：改寫或刪除 `APPLY_MIGRATION.md` 的 Studio 反模式，對齊 SOP。
2. seed：補建 `seed.sql`（或改 config.toml sql_paths 指向實際 seed 機制），讓 `db reset` 名實相符。
3. 草稿區：合併雙草稿機制為單一入口、清掉已落地版的草稿殘留。
4. `.skip` 檔：移出 `migrations/`（改放草稿區或刪），消除 audit_logs 雙來源歧義。
5. `CLEAN_MIGRATION.sql`：併入正式序列或刪除。
6. 時間戳衝突：補一支 audit 偵測同戳，新檔強制唯一戳；既有衝突若無相依可標記容忍、有相依則評估補一個顯式 ordering migration。
7. RLS：新建 table 強制走 3 個 procedure（已有 audit:rls，加嚴）。
8. 一次性運維資料：訂規則往後走獨立非 schema 通道，既有的標註於文件不改。

**規模**：M　**相依**：無

---

## 跨批次共用目錄注意（避免互踩）

- `src/lib/permissions/index.ts`：B6（路由映射）會動，B9 不要碰。
- `src/lib/constants/status-maps.ts`：B7（label 收斂）與 B8（constants 搬遷）需協調，建議 B7 先、B8 後。
- `src/data/core/*` 與 `src/stores/*`：B2（死碼）、B10（workspace_id）、B11（讀取層）都會碰，務必照 B2 → B10 → B11 順序、各自小 commit。
- `src/components/ui/*`：B2（_Input/use-toast 死碼）先清，B12 再做 primitive 收斂。
- `CLAUDE.md`：B5 定案後，B6/B11/B12 才寫入新規則。
