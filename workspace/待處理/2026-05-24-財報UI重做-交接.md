# 財報 UI 重做 — 交接（給新對話）

> Logan 2026-05-24 整理。新對話打開讀這份、照著做。

## 一句話

以線上「財報**規模化**」版為底，把 5/23 的「財報 **UI 重構**」（統一統計卡 + 明細小標題）**重新套上去** —— 保留規模化的資料載入、只改 UI 版型。**不是 git merge、是重做 UI。**

## 為什麼（背景）

- 5/23 有一批「財報 UI 重構」改動沒提交、留在本機 working tree。
- 5/24 抓 GitHub 最新時，發現線上這兩天把財報做了「規模化」（只撈選取範圍、效能優化、3 億規模用），動到同幾個檔 → 直接 pull 會衝突。
- William 拍板：**保留財報 UI、但不機械合併、而是「以線上規模化版為底、把 UI 重新改上去」**。
- UI 改動已用 stash 保存：`stash@{0}`「5/23 財報UI重構等未提交改動（待併回）」。

## 設計（William 5/23 拍板、兩個共用元件的註解都有記）

1. **`ReportStatCard`**（已建、在 working tree untracked）：統一統計卡。白卡底 + 細邊框 + hover 金、左標題右數字、不放 icon。比照「收支總覽」的 OverviewStatCards。**原本 6 個分頁各自重複的 StatCard（5 份）收斂成這 1 個。**
2. **`ReportSectionTitle`**（已建、untracked）：明細小標題。icon + text-xs 小標、**不包大卡框、直接接表格**。比照收支總覽明細區。
3. 6 個財報分頁版型統一、全部比照「收支總覽」的長相。

## 現況

- 本機 = 線上最新 `c8c0a9d`（已同步）。
- UI 改動在 `stash@{0}`。看完整參考：
  ```bash
  git stash show -p stash@{0}                              # 全部
  git diff "stash@{0}^1" "stash@{0}" -- <檔路徑>            # 單檔
  ```
- 兩個新元件**已在 working tree（untracked、可直接用）**：
  - `src/app/(main)/finance/reports/_components/ReportSectionTitle.tsx`
  - `src/app/(main)/finance/reports/_components/ReportStatCard.tsx`

## 7 個財報分頁狀態（stash 都改過 UI；線上只動了 3 個）

| 分頁               | 線上規模化動過？           | 做法                                               |
| ------------------ | -------------------------- | -------------------------------------------------- |
| `OverviewTab`      | ✅ 2a80311（只撈選取範圍） | **小心併**：保留線上資料載入、只把版型改套共用元件 |
| `DisbursementTab`  | ✅ 2a80311                 | 同上                                               |
| `IncomeTab`        | ✅ 47dca2e                 | 同上                                               |
| `BankBalancesTab`  | ❌ 線上沒動                | 直接照 stash 版重做 UI                             |
| `PayablesTab`      | ❌                         | 直接照 stash 版                                    |
| `ReceivablesTab`   | ❌                         | 直接照 stash 版                                    |
| `TourPnlTab`       | ❌（stash 改最多、278 行） | 直接照 stash 版                                    |
| `reports/page.tsx` | （stash 改 12 行）         | 對照 stash 改                                      |

## 做法步驟

1. 先跑 `venturo-page-scout` 探 `/finance/reports` 現況（動既有頁、憲法第 9 條）。
2. **4 個非衝突分頁**（Bank/Payables/Receivables/TourPnl）：線上沒動、對照 stash 把 UI 改套 `ReportStatCard` / `ReportSectionTitle`。
3. **3 個衝突分頁**（Overview/Disbursement/Income）：以**線上版（規模化資料載入）為底**，只把版型部分改套共用元件 —— **務必保留線上「只撈選取範圍」的資料邏輯**（那是效能關鍵）。
4. `reports/page.tsx` 對照 stash 改。
5. `npm run type-check` + `npm run lint` + `./scripts/check-standards.sh --strict` 全綠，跑起來確認 7 分頁版型統一 + 規模化沒破壞。

## ⚠️ 注意

- **`stash@{0}` 是「一整包」**、除了財報還夾帶非財報改動：
  - AI 設定（`AiSettingsDialog` / `AiSettingsTab` 刪除 / `LineSetup`）
  - layout（`content-container` 刪除 / `main-layout` / `SetupWizardLayout`）
  - `receipt.types.ts`、`websites-module-spec.md`
  - **這些非財報、先問 William 要不要**（本任務只做財報 UI、AI/layout 另議、不要順手帶進來）。
- **不要破壞線上規模化**（資料只撈選取範圍）—— 3 億規模的效能關鍵。
- UI 紅線：morandi design token、不用 Tailwind 預設色。

## 驗收

- 7 分頁版型統一（共用 `ReportStatCard` + `ReportSectionTitle`、比照收支總覽）
- 線上規模化資料載入**保留沒破壞**
- type-check / lint / check-standards 綠

---

## 附：永豐金流這條線的狀態（同期 Logan 做的、跟本任務無關、避免混淆）

- 永豐豐收款信用卡 A+B 階段 + 金額/時效/付款判斷修正**全部已 push 上線**（commit `0d3c7a0` / `7509314` / `641e333`、都在 `c8c0a9d` 之前）。
- 串接已用虛擬帳號驗證通（開單→自動付款→反查確認），信用卡卡在永豐 sandbox 未開通（待永豐窗口）。
- 永豐下一輪待辦：先存單再產連結 + 即時產連結（解 30 分鐘/留存/孤兒）+ receipt 狀態回寫 + 取消「再產一筆」+ Email 選填 + mock 拆除 + Banner。
- 本機有 4 支永豐除錯 script（untracked）：`scripts/sinopac-{smoke,query,autopay,atm}.ts`。
