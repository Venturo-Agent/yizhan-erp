# 夜間自動優化 — 交接 / 進度（建立 03:16）

> 這份是夜間 loop 的**唯一狀態來源 + 鐵律**。每次 wake-up 先讀這份、做下一個未勾項、回來更新勾選。
> 起點 commit：`87d55f9`（main，未 push）。loop 已於 03:56 完成。

---

## 🚦 BLOCKED / 待你決定清單（William 早上先看這段）

### 🔴 真 BLOCKED（撞牆、夜間不敢硬幹）
| # | 項目 | 卡在哪 | 解法 / 你要決定的 | 風險 |
|---|---|---|---|---|
| 1 | **A2** `orders/_quotes/_hooks/useQuickQuoteDetail.ts:49-56` — `else setItems([])` 閃爍 | prop→local state 鏡射。是否真閃取決於 parent 會不會 transient 傳 `quick_quote_items=undefined`。naive 修（拿掉 else 清空）會在報價真的清空項目時殘留舊項 = regression | 正解是 **lift state / 不鏡射 prop（重構）**；或先確認 parent 不會 transient undefined 再加 guard | 中（報價項目正確性）|

### 🟡 刻意沒做（需你決定 / 在場 / 動 production）
| # | 項目 | 為什麼夜間沒做 | 解鎖動作 | 風險 |
|---|---|---|---|---|
| 2 | **keepPreviousData on useList 家族** | 資安 gate。`clearAllSwrCacheKeys` 只清 localStorage **不清記憶體 SWR Map**；加了可能讓同台電腦切帳號時 B 短暫看到 A 的資料（紅線 G） | 先確認登入有強制重新掛載 (main)、**或**把 entity swrKey 綁 `workspace_id` 做隔離，再加 | 高（資安第一）|
| 3 | **apply migration `20260526120000`**（brands/checks 補 realtime publication）| 是 production 變更 | 你用 MCP `apply_migration` 跑（檔已寫好、idempotent、附 rollback）| 低（但動 production）|
| 4 | **DB 層 audit 本機跑不了** | Mac IPv6 不通、自動 skip | CI（有 `SUPABASE_DB_URL`）或 MCP 跑：`audit:realtime -- --db`、entity hook schema drift、FK 指向、L5 RLS | 低 |
| 5 | **B 層結構重構** | 需回歸測試、非 mechanical | 拍板順序（建議 B1→B2→B4→B3）。**B3（order_members 收回）必須你在場**。步驟在 `2026-05-26-B層執行步驟.md` | 中（B3 中-高）|
| 6 | **C 層大決策** | 結構/方向決策 | AI Hub 收回 entity hook、`src/data/hooks/` 逃生艙收編、`createReportHook` 定位、`CACHE_PRESETS` 做成真有差或拿掉、`audit-realtime.ts` 擴充、idb fallback 去留 | — |
| 7 | **UI component 檔** | 另一個 agent 的地盤、避免撞車 | 等 UI agent 跑完，從他改過的檔接手 | — |
| 8 | **push / deploy** | 無人看管不動 production | 你 review commit 序列後決定 push | — |

> 註：A1（TourPnlTab `setRows([])`）**不在此清單**——查證為誤報、當前行為正確、不該改。

---

## 背景
- 今天主線：讀取/快取/同步層全盤盤點（`workspace/健檢/pending/2026-05-26-讀取快取同步-全盤盤點.md`）。
- **另一個 agent 正在改 UI component 檔案**（git working tree 有 ~18 個我沒碰的 modified 檔：messages/zh-TW.json、library/*、tours/*、components/layout/*、action-cells、login/page、simple-order-table…）。**夜間 loop 絕不碰這些檔，避免撞車。**
- UI SSOT audit（另一 agent 產的）：`workspace/健檢/pending/2026-05-26-ui-ssot-audit.md`。

## 🔒 鐵律（每次 wake-up 必守）
1. **只做下面「✅ 可自動做」清單**。其他一律不碰。
2. **不碰另一 agent 正在改的 UI component 檔**（上面那 ~18 個 + 任何 `src/**/_components/**` 的 UI 重構）。只能動 lint 規則 / 純分析 markdown / 下面點名的孤立檔。
3. **不 push、不 apply migration、不部署**。commit 只到本地 main。
4. **不動 keepPreviousData、不動 RLS / auth / cache key 隔離**（資安第一，需 William 親自驗）。
5. 每次 code 變更 → `npm run type-check` + 對該檔跑 eslint，**過了才** `git add <該檔>` commit。失敗或有疑慮 → 還原該項、在本檔記 `BLOCKED: 原因`、跳下一項，**不硬幹**。
6. 做完所有「✅ 可自動做」→ **停止排程 + 發 push notification 叫 William**。

## ✅ 可自動做（安全、不撞車、可回復）

### 程式（孤立、不在另一 agent 的清單內）
- [x] **A1** `TourPnlTab.tsx:82` — ✏️ **誤報、不改**。`setRows([])` 只在 `tours.length===0`（真的沒團）的終端分支跑；effect 開頭沒清 rows（只 setLoading），refetch 期間舊資料保留、無中途閃爍。若硬「修」反而會在真的空結果時殘留舊列（regression）。當前行為正確。
- [BLOCKED] **A2** `useQuickQuoteDetail.ts:49-56` — prop→local state 鏡射 effect，`else setItems([])`。是否真閃取決於 parent 是否會 transient 傳出 `quick_quote_items=undefined`。naive 修（拿掉 else 清空）會在報價真的清空項目時殘留舊項（regression）。正解是 lift state / 不鏡射 prop = 重構，需 William 審。**不自動硬幹。**

### Lint 守門（只動 eslint-rules/ + eslint.config.mjs，不動 component 檔 → 不撞車；這是 UI audit 自己點名的根因「沒 ESLint 擋」）
- [x] **A3** ✅ 新增 `venturo/no-raw-input`（**warn**）。已驗證：在真實檔觸發、`src/components/ui/` + type=file/checkbox/radio/hidden/range/color 豁免、`npm run lint` exit 0（warn 不擋 CI）。全 repo 目前 **103 處 warn**（漸進清、配合 UI agent）。未動 baseline（warn 不需要）。
- [x] **A4** ✅ 完成（**改用擴充既有 `no-forbidden-classes`（已是 warn）而非新規則** — 重用其 className 掃描器、風險更低）。新增 pattern `(bg|text|border)-(red|green|blue|yellow|purple)-\d{2,3}` → 指向 status token。已驗證：inline 違規檔觸發（9）、**channel badge（CHANNEL_COLORS const map）天然豁免（0）**、全 repo 36 處 warn、`npm run lint` exit 0。

### 分析 / 報告（純新增 markdown、零風險）
- [x] **A5** ✅ 跑 audit:rls / writes / realtime / stale-refs（Mac code-grep fallback）。結果：code 層全綠（0 error）、無雙寫撞車、stale-refs 全綠；DB 層檢核 skip（需 CI/MCP）。彙整 → `workspace/健檢/reports/2026-05-26-夜間-audit彙整.md`，含「給 William 早上跑的 DB 層動作」。
- [x] **A6** ✅ B 層拆成可執行步驟 → `workspace/健檢/pending/2026-05-26-B層執行步驟.md`。B1(filter API)/B2(finance 失效)/B3(order_members 收回，需 William 在場)/B4(訊息樂觀+polling)，各含 file/改法/驗證/風險/是否需在場 + 建議順序 B1→B2→B4→B3。
- [x] **A7** ✅ **改用增補 doc、不直接改 Logan 週更新矩陣**（避免撞車/被覆蓋）。`workspace/健檢/reports/2026-05-26-讀取效能維度-矩陣增補.md`：指出「讀取效能」維度被高估（4–6 引擎、裸 useSWR 低估 2–3 倍）、逐 module 建議格子修正、今天加分項、待 DB 驗項。

## ⛔ 不自動做（prep-only，留給 William 早上審）
- keepPreviousData on useList 家族 — 資安 gate（clearAllSwrCacheKeys 只清 localStorage 不清 in-memory Map；需先確認登入重新掛載 / swrKey 綁 workspace_id）。
- apply `20260526120000_add_brands_checks_to_realtime_publication.sql` 到 production。
- B/C 結構性重構：收斂 4–6 套讀取引擎、ListView hooks 折回 entity hook、finance 32 個無失效寫入、AI Hub 訊息樂觀更新 + 收回 entity hook、重複 realtime 訂閱（tours/calendar）清理。
- 任何 UI component 檔的重構（另一 agent 的地盤）。
- push / deploy。

## 進度 log（每次 wake-up 追加）
- 03:16 建立交接、commit `87d55f9`（A 層 footgun×8 + lint no-mutate-clear + 紅線 I + 盤點 doc + migration 檔）。loop 啟動。
- 03:29 wake#1：A1 判定誤報（不改、會 regress）、A2 BLOCKED（需重構、不硬幹）、A3 完成（no-raw-input warn、103 處）。下一個：A4。
- 03:38 wake#2：A4 完成（擴充 no-forbidden-classes 加預設語意色 pattern、36 處 warn、channel badge 天然豁免、lint exit 0）。下一個：A5（跑 audit + 彙整）。
- 03:45 wake#3：A5 完成（4 個 audit code 層全綠、彙整報告 + DB 層待跑清單）。下一個：A6（B 層執行步驟清單）。
- 03:50 wake#4：A6 完成（B 層執行步驟 doc、B1-B4 + 順序）。下一個：A7（5 維度矩陣更新）。
- 03:56 wake#5：A7 完成（讀取效能維度增補 doc）。**「✅ 可自動做」全部完成。停止排程、發推播通知 William。**

---

## ✅ 夜間 loop 完成總結（03:56）
- **A 層核心已 commit**（main、未 push）：footgun×8 + no-mutate-clear(error) + 紅線 I + 盤點 doc + brands/checks migration 檔。
- **UI 守門**：no-raw-input(warn 103) + no-forbidden-classes 加預設語意色(warn 36)。
- **分析交付**：audit 彙整、B 層執行步驟、讀取效能維度增補。
- **未做（留 William）**：A1 誤報不改、A2 BLOCKED（需重構）、所有 ⛔ prep-only 項（keepPreviousData/apply migration/B-C 結構/UI component/push）。
- commit 序列：`87d55f9`→`253107c`→`70511bc`→`87cf356`→`ff54a64`→`83c2deb`→（本輪 A7）。
