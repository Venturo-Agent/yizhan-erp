---
date: 2026-05-15
type: Loop fix log
rounds: 6-8
owner: Logan
---

# QDF Loop Rounds 6-8 fix log

## Round 6 — 跑全 audit + restart 通報

跑完所有 5 個 audit 工具確認當前狀態：
- audit:capability-coverage: 138 / 138 全綠 ✅
- audit:flow-strictness: 必修 0 / 建議 15（false positive）
- audit:data-consistency: 0 finding ✅
- audit:ui-consistency: Dialog 53.8% / Layout 70.3%
- audit:spec-coverage: 0%（命名 mismatch、不是真缺）
- unit test: 42 / 42 全綠

## Round 7 — RefundReceiptDialog 遷 FormDialog

**Baseline**：RefundReceiptDialog 直接用 ui/dialog、不走 SSOT

**修補**：
- swap 為 `<FormDialog>` from `@/components/dialog/form-dialog`
- handleSubmit 從 `e.preventDefault()` 改成直接 callable（FormDialog footer 模式）
- footer 自訂保留 danger 紅按鈕（退款）
- 順手把 `new Date().toISOString().split('T')[0]` → `formatDateTaipei(new Date())`

**結果**：
- audit:ui-consistency Dialog SSOT 53.8% → 55.0%
- build / restart OK

## Round 8 — audit:spec-coverage 寬鬆 match

**Baseline**：spec-coverage 0%（嚴格 regex 不符即便 spec 實際存在）

**修補**：
- 加 `MODULE_KEYWORD_OVERRIDE` mapping：每 module 對應一組關鍵字
- 譬如 `hr_bonus_settlement` 對應 `'bonus-settlement'` / `'bonus_settlement'`
- 譬如 `tours` 對應 `'tours'` / `'旅遊'` / `'出帳'` / `'結團'`
- 中文關鍵字也支援

**結果**：
- audit:spec-coverage 0% → 28.6%（8 / 28 已有 spec）
- 剩 20 個真實缺 spec 的 module（待補）

## Loop 累積總結（Rounds 1-8）

### 新建 audit 工具（5 個、永久資產）
1. audit:capability-coverage（138 endpoint 全綠）
2. audit:flow-strictness（金流 audit log / race）
3. audit:data-consistency（formatter SSOT）
4. audit:ui-consistency（Dialog / Layout SSOT 覆蓋率）
5. audit:spec-coverage（module spec 對應）

### 新建 SSOT helper（2 個）
- formatDateTaipei
- distributeFees（fee-distribution 模組）

### 新建 unit test（42 tests）
- leave-severance-calculator (31)
- fee-distribution (11)

### 修補 code 檔（~30 個）
- 紅線 error（schema drift / error.message 洩露）：6 個
- 守門補上（capability / audit log）：12 個
- 日期 / 一致性 swap：11 個
- batch-create swap fee-distribution：1 個
- RefundReceiptDialog 遷 FormDialog：1 個

### QDF 框架文檔
- README.md
- 7 維度 finding（01-07）+ 4 維度 blueprint（01, 02, 03, 07）
- _spec-template.md（10 段標準範本）
- _audit-runs/2026-05-15-baseline.md
- _fix-logs/2026-05-15-rounds-2-5.md
- _fix-logs/2026-05-15-rounds-6-8.md（本檔）

## Current state（8 rounds 後）

| metric | baseline | now |
|--------|----------|-----|
| capability coverage | 32 未守 | 0 ✅ |
| flow-strictness 必修 | 10 | 0 ✅ |
| data-consistency | 14 散刻 | 0 ✅ |
| UI Dialog SSOT | 53.8% | 55.0% ⬆ |
| spec coverage | 0% | 28.6% ⬆ |
| unit test | 0 | 42 ✅ |

## 下輪規劃

最少優先 / 最有 ROI：
- 補 module spec（20 缺 spec 的）
- UI 遷移（剩 24 個 dialog）
- payroll-engine unit test
- migration rollback 補（750 個只 57 個有）

工具自動化（Loop 進深）：
- CronCreate 半夜跑 audit
- 半夜 fix loop（小違規 auto fix）
