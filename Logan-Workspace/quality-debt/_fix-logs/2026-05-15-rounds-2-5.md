---
date: 2026-05-15
type: Loop fix log
rounds: 2-5
owner: Logan
---

# QDF Loop Rounds 2-5 fix log

承接 round 1 baseline、進 loop mode 持續做。

## Round 2 — #3 資料一致：formatDateTaipei swap

**Baseline**：14 處日期散刻

**修補**：
- 新 helper：`formatDateTaipei(date)` SSOT（用 sv-SE + Asia/Taipei）
- swap 8 處 `toISOString().slice(0, 10)`：
  - `src/app/(main)/ai/_components/AiConversationsTab.tsx`
  - `src/app/(main)/workspaces/[id]/_components/billing-tab.tsx`
  - `src/lib/ai/context-builder.ts`
  - `src/lib/facebook/setup-pipeline.ts`
  - `src/lib/instagram/setup-pipeline.ts`
  - `src/lib/line/erp-bridge.ts`（2 處）
  - `src/lib/line/setup-pipeline.ts`
- swap 6 處 `toLocaleDateString('sv-SE', timeZone)`：
  - `src/app/(main)/calendar/_hooks/useEventOperations.ts`（5 處）
  - `src/app/(main)/bot/[lineUserId]/_components/CustomerInfoSidebar.tsx`（1 處、用 formatDate）

**結果**：audit:data-consistency 14 → 0 ✅

## Round 3 — #6 測試：leave-severance unit test

**Baseline**：critical service 0% unit test 覆蓋

**修補**：
- 新建 `src/lib/hr/__tests__/leave-severance-calculator.test.ts`
- 31 test cases、涵蓋：
  - annualLeaveDaysByTenure（8 個邊界 case）
  - calcYearsOfService（4 個 case）
  - calcAnnualLeave（週年 vs 年度制、3 case）
  - calcSeverance（new / old / mixed 三制、8 case）
  - calcAvgMonthlyWage / estimateAvgWageFromSalaryInfo（8 case）

**結果**：✅ 31 / 31 全綠

## Round 4 — #6 測試 + 抽象：fee-distribution helper

**Baseline**：手續費分攤算法 inline 在 batch-create/route.ts、無測試

**修補**：
- 新建 `src/lib/disbursement/fee-distribution.ts`
  - export `distributeFees(input)` pure function
  - 涵蓋 average / unified mode + equal / proportional strategy
- 新建 `src/lib/disbursement/__tests__/fee-distribution.test.ts`
  - 11 test cases、涵蓋邊界（0 fee / 全同行 / cross+same 混合 / unified 公司虧 / cross 金額 0）
- `src/app/api/disbursement/batch-create/route.ts` swap：
  - inline 分攤算法 → `distributeFees(...)` helper

**結果**：
- 42 / 42 unit tests 全綠
- build OK、無回歸

## Round 5 — #5 文檔：audit:spec-coverage 工具

**Baseline**：spec 覆蓋率不明、命名 convention 混亂

**修補**：
- 新建 `scripts/audit-spec-coverage.ts`：盤點 28 個 module 哪些有 spec
- 發現：所有 28 個 module 「audit 視為」缺 spec
  - **原因**：spec 命名沒對齊 module code（譬如 hr_bonus_settlement vs `2026-05-15-bonus-settlement-spec.md`）
- 新建 `Logan-Workspace/quality-debt/_spec-template.md`：10 段標準範本
- finding：需要 spec 命名規範 + 補 28 個 module spec（之後分批做）

**結果**：工具 ship、留 finding 進下輪

## 累積 deliverable（rounds 1-5）

### 新建 audit 工具（永久資產）
1. `scripts/audit-capability-coverage.ts`
2. `scripts/audit-flow-strictness.ts`
3. `scripts/audit-data-consistency.ts`
4. `scripts/audit-ui-consistency.ts`
5. `scripts/audit-spec-coverage.ts`

### 新建 SSOT helper
1. `src/lib/utils/format-date.ts::formatDateTaipei`
2. `src/lib/disbursement/fee-distribution.ts::distributeFees`

### 新建 unit test
1. `src/lib/hr/__tests__/leave-severance-calculator.test.ts`（31 tests）
2. `src/lib/disbursement/__tests__/fee-distribution.test.ts`（11 tests）

### 修補檔（共 ~30 個）
- orders.code 已砍 caller（2 個）
- API error.message → dbErrorResponse（2 個）
- API endpoint 補守門（3 個）
- API 金流 endpoint 補 audit log（9 個）
- 日期散刻 swap formatDateTaipei（11 個）
- batch-create swap fee-distribution helper（1 個）

### 文檔（QDF 框架）
- `Logan-Workspace/quality-debt/README.md`
- 7 維度 finding 文件
- 4 維度 blueprint 文件
- `_spec-template.md`
- baseline + fix-log

## 下輪規劃（Round 6+）

可選方向：
1. **#5 文檔深做**：建 5 個 module spec（用範本、選 tours / orders / finance / hr / disbursement）
2. **#6 測試深做**：再寫 1-2 個 critical service unit test（payroll-engine / bonus 計算）
3. **#7 UI 遷移**：遷 1-2 個 dialog 到 FormDialog
4. **CronCreate Loop**：設半夜 audit 自動跑、寫每日 diff
