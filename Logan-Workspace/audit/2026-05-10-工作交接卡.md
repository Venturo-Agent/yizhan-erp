---
created: 2026-05-10
purpose: compact 後 / 新 session 接手用、交接當前進度跟下一步
status: in-progress（5/8 批完成）
---

# venturo-aierp 整合工程交接卡

> compact 後的新 session 從這裡接手。包含「已完成」「剩下要做」「關鍵 context」。

---

## 老大的最終目標

讓 venturo-aierp 像新的一樣乾淨 → 連 Supabase → 寫測試帳號 → 實測。

工程分 8 批、目前完成 5/8。

---

## 已完成（11 個 commit）

```
12b1913 tours/quotes/itinerary 整合 + 砍 src/features/ 整個資料夾
fd15adb disbursement/orders/finance 整合
21176b2 hr/todos/dashboard/calendar 整合
eb38d20 tenants→workspaces + unauthorized→no-access 改名
5320934 library 主頁 + 砍 database 整個資料夾
c5868e5 customers 搬 library/customers
6b320f5 archive-management 整合
dfcd23e attractions 整合
b4b30c2 重寫 CLAUDE.md（加哲學原則 + 技術紅線）
4655692 拿掉舊紅線 section
fe897df suppliers 整合
```

**里程碑**：
- ✅ `src/features/` 整個資料夾消失（從 14 個模組 → 0）
- ✅ 路由收斂：`/database` → `/library`、`/tenants` → `/workspaces`、`/unauthorized` → `/no-access`
- ✅ 子母結構完成：每個功能在 `src/app/(main)/<route>/_components/_hooks/_services/_types/` 底下
- ✅ 跨路由共用展示元件：`src/components/tour-display/`（給 `/view/[id]` 公開分享頁用）
- ✅ 累計刪除 ~76000 行（雙倉庫 + 舊 database 殘留）

**未 push**（commit 在 main、ahead of origin/main 多個 commit）

---

## 剩下要做（3 批）

### 第 6 批：CIS 移平臺側
**範圍**：把 `src/app/(main)/cis/` 從 ERP 側移到平臺側
**說明**：
- 老大規劃 CIS 是漫途自家服務、給「平臺管理員」（漫途超管）用、不給租戶
- 平臺側目前只有 `/workspaces`（租戶管理）
- CIS 要跟 workspaces 同層
- 路徑可能不變（還是 `/cis`）、但 sidebar 要分流（CIS 只在平臺管理員看得到）
- 守門：加進 `PLATFORM_CAPABILITY_ROUTES`（目前是 `['/workspaces']`、加 `/cis`）

**檔案**：
- `src/app/(main)/cis/` 整個資料夾（page.tsx + [id]/page.tsx + pricing/page.tsx）
- `src/components/layout/sidebar.tsx`（CIS entry 改成平臺專屬）
- `src/lib/permissions/index.ts` PLATFORM_CAPABILITY_ROUTES 加 `/cis`
- `src/components/guards/ModuleGuard.tsx` 同步

### 第 7 批：docs 對齊現況
**範圍**：把舊文件改寫對齊新狀況
- `docs/SITEMAP.md` 跟 `docs/SITEMAP-OVERVIEW.md` 合併（兩份矛盾、要砍一份）
- `docs/ARCHITECTURE.md` 把 features/ 是 SSOT 的描述刪掉（已不存在）
- `docs/SECURITY.md` 把虛構的 P100 detector 拿掉（程式裡沒實裝）
- `docs/AGENT-GUIDELINE.md` 過時段更新
- `docs/META-RULES.md` 過時 metadata 更新（提到 venturo-erp 改 venturo-aierp）
- `docs/routes/` 補新路由 stub（library / workspaces / no-access、砍 database / tenants / unauthorized stub）

### 第 8 批：雜項清理
- `supabase/config.toml:5` `project_id = "venturo-new"` 改成新 project name
- `scripts/db-migrate.js` / `export-db.js` 驗證內容（reset-db.js 跟 import-db.js 是空檔可砍）
- Memory 系統清理：`feedback_red_line_corner_data_only.md`（CORNER row 紅線、舊 venturo-erp 用、新專案不適用）

---

## 關鍵 context（compact 後需要記得）

### 老大要的協作模式

1. **中文業務語言為主、術語加註解**
2. **William 不看 code、所以每件事要說清楚為什麼**
3. **分進度讓 Subagent 跑、主對話只做檢測 + 拍板對接**
4. **紅線錯誤時必先派分身查、不憑單一字串下結論**
5. **不寫 secret value、只寫位置**（在 `~/.config/venturo/secrets.env`）
6. **MCP 不是第一手、先讀 INFRASTRUCTURE.md**

詳見：`/Users/william/Projects/venturo-aierp/CLAUDE.md`（已重寫、保留這 6 條哲學原則）

### Infrastructure 速查

- 所有 secret 在 `~/.config/venturo/secrets.env`
- 完整索引在 `~/.claude/INFRASTRUCTURE.md`
- venturo-aierp Supabase ref：`aawrgygqgemgqssflfrx`（agency@venturo.tw 帳號、**MCP 看不到**、走 secrets.env + REST 或 Vultr psql）
- 舊 Venturo-Erp Supabase：`wzvwmawpkapcmkfmkvav`（William 個人帳號、MCP 通）
- Mac 沒 IPv6、不能直連 Supabase db host

### 紅線重寫備忘

`/Users/william/Obsidian/Logan-Workspace/audit/2026-05-10-紅線重寫備忘.md` — 已重寫進新版 CLAUDE.md

### 新版 CLAUDE.md 結構（commit b4b30c2）

```
## 優先順位（資安 #1 → 效能 #2 → SSOT #3）
## 寫程式哲學（協作原則 6 條）
## 五大方向（路由 / GitNexus / UI / RLS / 連線）
## 技術紅線（A: FORCE RLS / B: 審計 FK / C: Admin client per-request）
## Build / Commit 規則
## 工具參考（含 secrets.env + REST 替代 MCP）
## GitNexus 段（改 optional + 索引可能 stale）
```

### 已知既存問題（pre-existing、不在當前 scope）

- `check-standards.sh #6` 違反：3 處 `deleted_at` 註解（tours/_hooks + finance/disbursement service + enforce-workspace-scope）
- `tests/lib/permissions/index.test.ts` 在 commit eb38d20 後從 3 fail 變 0 fail（PLATFORM_CAPABILITY_ROUTES 加 `/workspaces` 後修了）
- `tests/e2e/full/other-pages.spec.ts:108-114` dead test（測 /database/fleet、不存在）
- 12 處 audit FK 違規（指 auth.users 不指 employees）— 老大說 Supabase 還沒寫表、之後重寫時用對的

### 沒帳號的問題

老大說新 Supabase 連線**沒員工帳號可登入測試**、所以瀏覽器實測跑不了。我用 type-check + lint + check-standards 驗證每批 commit。實測等第 8 批完成後寫測試帳號 seed 才能跑。

### Capability code 沒改

`/database` 路由改 `/library`、但 capability module code **仍是 `'database'`**（綁 DB schema、要改要動 migration、屬另一批）。

---

## 下一個 session 的開場建議

1. Read 這份交接卡了解進度
2. 跑 `git log --oneline -15` 確認 commit 狀態
3. Telegram 老大「我從交接卡接手、要繼續第 6 批 CIS 移平臺側嗎？」
4. 等老大確認、派 subagent

**規則約束（不變）**：
- 不准 commit / push 沒老大授權
- 不准 --no-verify / as any
- 不寫 secret value
- 用中文業務語言為主

---

## 用過的 subagent / 工具歷史

- 大量 general-purpose subagent 做整合工程（每個模組 1 subagent）
- 不用 mcp__supabase（venturo-aierp 用 agency@ 帳號、看不到）
- 不用 GitNexus（索引可能 stale）
- 用 Bash + Edit + Write + Read 為主
- Telegram reply 跟老大溝通

---

**這份交接卡讓 compact 後的新 session 5 分鐘上手。**
